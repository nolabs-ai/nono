---
phase: 30-windows-nono-shell-architecture
plan: 05
type: execute
wave: 4
depends_on:
  - 04
files_modified:
  - .planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/debug/nono-shell-status-dll-init-failed.md
  - .planning/debug/resolved/nono-shell-status-dll-init-failed.md
  - docs/cli/development/windows-poc-handoff.mdx
  - crates/nono-cli/src/supervised_runtime.rs
autonomous: false
conditional_execution:
  trigger_signal: "Plan 30-04 Checkpoint 3 ∈ {wave2-trigger-launch, wave2-trigger-tui, wave2-trigger-write-deny}"
  skip_signal: "Plan 30-04 Checkpoint 3 = success-path"
  rationale: "Wave 2 ProcMon investigation runs ONLY when Wave 1 field smoke surfaced a failure. CONTEXT.md D-04: timebox 3-5 working days. Goal: surface a sixth option from actual Win32 mechanism, NOT a token-shape iteration."
requirements:
  - D-04
  - D-07
  - D-10
tags:
  - windows
  - procmon
  - wave-2-investigation
  - kernel-driver-deferral-candidate
  - human-checkpoint

must_haves:
  truths:
    - "D-04: Wave 2 timebox is 3-5 working days. The phase ships at end-of-timebox regardless of investigation depth — either with a sixth option implemented OR with a documented v3.0/kernel-driver deferral"
    - "D-04: ProcMon trace recipe per RESEARCH § ProcMon Trace Plan: filters on conhost.exe / powershell.exe / cmd.exe + paths \\Device\\ConDrv, \\BaseNamedObjects, \\Sessions\\; operations Process Create + Result != SUCCESS; records as .pml"
    - "D-04: Output is NOT a token-shape iteration. Investigations target Win32 mechanism — \\Device\\ConDrv ALPC, \\BaseNamedObjects per-session DACLs, conhost handshake — and propose a fix at that layer (e.g., per-session named-object DACL edit, or sequencing fix, or broker-process pattern)"
    - "D-07 (failure path): On exhaust-without-fix outcome, cookbook reverts per RESEARCH §Cookbook Rollback Path Option Rev-B (text replacement, NOT git revert): strip the <Note> recommendation, Step 4 instruction, Step 5 interactive-verification block, and Step 6 row; KEEP the Known limitation section (lines 233-239); add a 'nono shell on Windows is deferred to v3.0' section"
    - "D-10 (failure path): SHELL-01 PROJECT.md row flips to ✘ deferred to v3.0; STATE.md Key Decisions documents the failure-mode evidence; debug session moves to resolved/ with Resolution section recording 'phase shipped failure-mode finding'"
    - "D-10 (success path): If Wave 2 surfaces a sixth option AND it implements cleanly, SHELL-01 → ✔ validated v2.X Phase 30 with sixth-option narrative; debug session moves to resolved/"
    - "Plan 30-05 owns the FINAL state of all artifacts touched in Wave 1 prelude/edit/smoke (PROJECT.md, STATE.md, debug session, cookbook). Plan 30-04 left them at intermediate Wave-2-trigger state; Plan 30-05 ships the terminal state"
  artifacts:
    - path: ".planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md"
      provides: "ProcMon trace plan + filter recipe + collected evidence + sixth-option synthesis OR documented exhaust-without-fix outcome"
      contains: "ProcMon"
    - path: ".planning/PROJECT.md"
      provides: "SHELL-01 row final state — ✔ validated v2.X Phase 30 with sixth-option narrative OR ✘ deferred to v3.0"
      contains: "SHELL-01"
    - path: ".planning/STATE.md"
      provides: "Phase 30 Wave 2 outcome entry; stopped_at: Phase 30 complete OR Phase 30 deferred"
      contains: "Phase 30"
  key_links:
    - from: ".planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md"
      to: ".planning/debug/resolved/nono-shell-status-dll-init-failed.md (Resolution section)"
      via: "narrative reference + ProcMon evidence cross-link"
      pattern: "Wave 2"
    - from: "PROJECT.md SHELL-01 row (final)"
      to: "STATE.md Phase 30 entry (final)"
      via: "phase reference"
      pattern: "Phase 30"
---

<objective>
**Conditional Plan.** Executes ONLY when Plan 30-04 Checkpoint 3 selected one of: `wave2-trigger-launch`, `wave2-trigger-tui`, `wave2-trigger-write-deny`. Skipped when `success-path`.

Per CONTEXT.md D-04: ProcMon-driven Win32 investigation. Timebox 3-5 working days. Goal: surface a sixth option from the actual Win32 mechanism — `\Device\ConDrv` ALPC port DACL, `\BaseNamedObjects` per-session subdir access, conhost handshake — NOT a token-shape iteration.

Three sequential phases:

1. **Investigation phase (Tasks 1-3):** ProcMon trace setup; failure reproduction with Plan 30-03's harness scripts; trace analysis to identify the failing Win32 surface (failed ALPC operation, denied named-object access, ImageLoad chain DllMain failure, etc.).

2. **Hypothesis phase (Task 4):** Synthesize the trace evidence into a sixth-option fix proposal. Examples per RESEARCH §"What 'surfaced a 6th option' looks like":
   - Per-session `\BaseNamedObjects\<X>` DACL ACE granting Low-IL access (admin-install requirement).
   - Sequencing fix: create pseudoconsole BEFORE dropping integrity (in `pty_proxy::open_pty()` callers).
   - Broker-process pattern: pre-load CLR/System32 DLLs into the supervisor, inject by hand into the child.

3. **Implementation OR exhaust-without-fix phase (Tasks 5-6):** Either implement the surfaced fix and re-run Plan 30-03's harness to verify, OR document the exhaust-without-fix outcome and ship Phase 30 as v3.0 / kernel-driver deferral.

Whichever the outcome, this plan ships Phase 30's terminal state — PROJECT.md, STATE.md, cookbook, debug session all reach their final form here. Plan 30-04 left them at intermediate "Wave 2 in flight" state.

Purpose: Either surface a sixth option that closes Phase 30 cleanly, OR honestly defer to v3.0 with cookbook revert and bookkeeping coherence. Both outcomes leave the project in a coherent state.
Output: Definitive Phase 30 close — validated OR deferred. No middle state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/30-windows-nono-shell-architecture/30-CONTEXT.md
@.planning/phases/30-windows-nono-shell-architecture/30-RESEARCH.md
@.planning/phases/30-windows-nono-shell-architecture/30-PATTERNS.md
@.planning/phases/30-windows-nono-shell-architecture/30-FIELD-SMOKE.md
@.planning/debug/nono-shell-status-dll-init-failed.md
@scripts/test-windows-shell-write-deny.ps1
@scripts/test-windows-shell-tui.ps1
@docs/cli/development/windows-poc-handoff.mdx
@crates/nono-cli/src/supervised_runtime.rs
@crates/nono-cli/src/exec_strategy_windows/launch.rs
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Set up ProcMon trace environment + reproduce failure</name>
  <what-built>
    Plan 30-04 surfaced a Wave 1 field-smoke failure. Plan 30-05 begins ProcMon investigation per RESEARCH § ProcMon Trace Plan (lines 209-263). ProcMon UI configuration is manual; this checkpoint pauses Plan execution while the operator sets up filters and captures a baseline trace.
  </what-built>
  <how-to-verify>
On the Windows test box:

1. Download Sysinternals ProcMon and Process Explorer if not already installed:
   - https://learn.microsoft.com/en-us/sysinternals/downloads/procmon
   - https://learn.microsoft.com/en-us/sysinternals/downloads/process-explorer

2. Launch ProcMon as Administrator. The default capture starts immediately; press CTRL+E to toggle data collection OFF until filters are applied.

3. Apply the filter recipe from RESEARCH § "Filter recipe for ConPTY + restricted-token failures" (lines 232-243):
   ```
   Process Name is conhost.exe
   Process Name is powershell.exe
   Process Name is cmd.exe
   Path contains \Device\ConDrv
   Path contains \BaseNamedObjects
   Path contains \Sessions\
   Operation is Process Create
   Result is not SUCCESS
   ```
   Use Filter → Filter (CTRL+L) to enter each criterion. Use the OR semantics (multiple include rules ⇒ any match) for Process Name; AND for the conditions filtering by path/operation/result (multiple include + multiple exclude semantics).

4. Press CTRL+X to clear the buffer. Press CTRL+E to start capture.

5. Reproduce the Wave 1 failure surfaced in Plan 30-04 Checkpoint 3. The reproducer command depends on the failure category:
   - `wave2-trigger-launch` — `pwsh -File scripts/test-windows-shell-write-deny.ps1` (launches and exits 0xC0000142). The harness build step + the failed launch are both captured.
   - `wave2-trigger-tui` — `pwsh -File scripts/test-windows-shell-tui.ps1` and follow the operator instructions until the Pitfall-2 input-drop / broken-echo failure surfaces. Capture the moment of failure.
   - `wave2-trigger-write-deny` — `pwsh -File scripts/test-windows-shell-write-deny.ps1`; observe the inner `Out-File` succeeding when it should be denied.

6. Press CTRL+E to stop capture immediately after the failure reproduces.

7. Save the trace as `.pml` to `.planning/phases/30-windows-nono-shell-architecture/wave2-procmon-baseline-<YYYYMMDD>.pml` (use CTRL+S, choose "Native Process Monitor Format (PML)"). The `.pml` file is binary; do NOT commit it to git. Add `*.pml` to `.gitignore` if not already present, OR copy a CSV export into the working tree for evidence (CSV exports via File → Save As → CSV).

8. Open the trace and use the Filter view to identify:
   - The first `Result != SUCCESS` row in chronological order — that's the proximate cause.
   - The corresponding `Process Create` events — token attributes, parent process, command line.
   - For `wave2-trigger-launch`: the ImageLoad sequence in conhost.exe immediately preceding the child's silent exit. Identify the first DLL with Result != SUCCESS — that's equivalent to Phase 15's CLR-DllMain framing.

Record:
- ProcMon installation status.
- Filter configuration (paste from `.pmf` filter export OR screenshot).
- Reproducer command used.
- Trace file path (`.pml` binary OR `.csv` export).
- The first Result != SUCCESS row's: Process, Operation, Path, Result, Detail.
  </how-to-verify>
  <resume-signal>Type one of:
- `procmon-trace-captured` — Trace recorded; first failure row identified; ready for analysis (Task 2)
- `procmon-trace-clean` — Trace recorded but NO failures captured (suggests intermittent; re-run Wave 1 harness to provoke)
- `procmon-blocked` — Test box unavailable, ProcMon install failed, can't reproduce after multiple attempts
  </resume-signal>
  <files>(checkpoint task — no file modifications by Claude; operator runs harness on Windows test box and reports results)</files>
  <action>This is a checkpoint task. The operator follows the steps in &lt;how-to-verify&gt; (or &lt;decision&gt; / &lt;options&gt; for decision checkpoints) and types the appropriate &lt;resume-signal&gt; phrase to resume the plan. The plan branches on the resume-signal value. No autonomous Claude edits in this task.</action>
  <verify>
    <automated>echo "checkpoint:human task — verify by operator-typed resume-signal value matching one of the documented options"</automated>
  </verify>
  <done>Operator has typed a recognized resume-signal value; downstream tasks branch accordingly.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Analyze trace; identify failing Win32 surface</name>
  <what-built>
    Task 1 captured a ProcMon trace at the moment of Wave 1 failure. Task 2 analyzes the trace to identify the proximate Win32 mechanism — failed ALPC port operation, denied named-object access, DllMain failure, etc.
  </what-built>
  <how-to-verify>
On the Windows test box (or a Windows host with the saved `.pml` file):

1. Open the saved `.pml` trace in ProcMon.

2. Filter to `Result != SUCCESS` (already configured if Task 1 filters were applied). The first chronological row is your starting point.

3. For each failure row, capture:
   - **Process Name:** which process saw the failure (conhost.exe? the shell child? the supervisor?).
   - **PID + parent PID:** establish the process tree for the failure context.
   - **Operation:** Process Create / CreateFile / IRP_MJ_CREATE / Load Image / etc.
   - **Path:** what resource was being accessed.
   - **Result:** ACCESS_DENIED / STATUS_INVALID_HANDLE / STATUS_OBJECT_NAME_NOT_FOUND / etc.
   - **Detail:** desired access + granted access + impersonation level.

4. Right-click the failure row → "Stack" to view the call stack. Capture the user-mode + kernel-mode frames if Sysinternals symbol resolution is configured (`File → Configure Symbols → _NT_SYMBOL_PATH=srv*c:\symbols*https://msdl.microsoft.com/download/symbols`).

5. Cross-reference with Process Explorer:
   - Find the failing process; right-click → Properties → Security tab.
   - Capture Token Integrity Level (Low / Medium / Untrusted) — confirms the cascade arm took effect.
   - Capture Token Restricted Sids list — confirms the Low-IL token does NOT carry per-session SID.

6. Map the failure surface to one of RESEARCH § "Events to look for" categories:
   - **Failed `\Device\ConDrv` operations:** ALPC port DACL denials. Conhost can't accept Low-IL connections for input/output.
   - **`\BaseNamedObjects\` per-session access denials:** section objects, mutexes, events under `\Sessions\<n>\BaseNamedObjects\` with Low-IL-excluding DACLs.
   - **`Process Create` chain success but immediate child exit:** ImageLoad in the child reveals the failed DllMain (the 0xC0000142 trigger).
   - **Other:** something not in the above categories — record details, escalate.

Record findings to `.planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md` (created in this task). Use this template:

```markdown
# Phase 30 Wave 2 ProcMon Investigation

**Date:** YYYY-MM-DD
**Trigger:** Plan 30-04 Checkpoint 3 = <wave2-trigger-launch | wave2-trigger-tui | wave2-trigger-write-deny>
**Operator:** <name>
**Trace file:** .planning/phases/30-windows-nono-shell-architecture/wave2-procmon-baseline-<DATE>.pml (NOT committed; CSV export at <path>)

## Failure surface

**Failing process:** <process name + PID>
**Token integrity level:** <Low / Medium / Untrusted>
**Token restricted SIDs:** <count + brief description>
**Operation:** <Process Create / CreateFile / Load Image / ALPC ...>
**Path:** <resource path>
**Result code:** <NTSTATUS or Win32 error>
**Call stack (top 10 frames if symbols available):** <paste>

## RESEARCH category match

<one of:>
- \Device\ConDrv ALPC denial
- \BaseNamedObjects per-session access denial
- ImageLoad / DllMain failure
- Other (describe)

## Hypothesis space (Task 4 fills in candidates)

(empty for now)

## Sixth-option proposal (Task 4 fills in)

(empty for now)
```

7. If the trace shows the failure but doesn't clearly localize it (multiple Result != SUCCESS rows; unclear root cause), iterate: refine filters, re-capture, re-analyze. Within the 3-5 working day timebox, two or three trace iterations are reasonable.

Record:
- Number of failure rows in the trace.
- The proximate failure (the first chronological Result != SUCCESS row that maps to the Wave 1 failure category).
- RESEARCH category match.
- Notes on iteration / refinement.
  </how-to-verify>
  <resume-signal>Type one of:
- `analysis-localized` — Single proximate failure identified; matches a RESEARCH category; ready for hypothesis synthesis (Task 4)
- `analysis-multi` — Multiple plausible failure surfaces; need to disambiguate via more targeted re-trace (re-run Task 1 with refined filters)
- `analysis-novel` — Failure surface doesn't match any RESEARCH category; escalate / extend RESEARCH
- `analysis-no-failure` — Trace shows no SUCCESS-failing rows even though Wave 1 reproducer fails. Filters may be wrong; revisit Task 1.
  </resume-signal>
  <files>(checkpoint task — no file modifications by Claude; operator runs harness on Windows test box and reports results)</files>
  <action>This is a checkpoint task. The operator follows the steps in &lt;how-to-verify&gt; (or &lt;decision&gt; / &lt;options&gt; for decision checkpoints) and types the appropriate &lt;resume-signal&gt; phrase to resume the plan. The plan branches on the resume-signal value. No autonomous Claude edits in this task.</action>
  <verify>
    <automated>echo "checkpoint:human task — verify by operator-typed resume-signal value matching one of the documented options"</automated>
  </verify>
  <done>Operator has typed a recognized resume-signal value; downstream tasks branch accordingly.</done>
</task>

<task type="auto">
  <name>Task 3: Document trace findings to 30-WAVE-2-PROCMON.md</name>
  <files>.planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md</files>
  <read_first>
    - The Task 2 findings (operator's notes from the checkpoint resume signal)
    - .planning/phases/30-windows-nono-shell-architecture/30-RESEARCH.md § "ProcMon Trace Plan for Wave 2" (lines 209-263)
    - .planning/phases/30-windows-nono-shell-architecture/30-FIELD-SMOKE.md (Wave 1 baseline that Wave 2 builds on)
  </read_first>
  <action>
Materialize Task 2's manual notes into a committed document at `.planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md`. The document is the single canonical record of Wave 2 investigation evidence. Plan 30-05's later tasks (4, 5, 6) reference it.

Use the template from Task 2's `how-to-verify` section. Fill in the sections from the operator's notes:
- **Failure surface** — process, token IL, operation, path, result code, call stack.
- **RESEARCH category match** — which of the 4 categories the failure matches.

Keep the **Hypothesis space** and **Sixth-option proposal** sections present but empty (Task 4 fills them).

Add a section at the bottom:

```markdown
## Trace evidence files

- `.pml` binary: `<path>` (NOT committed — gitignored)
- `.csv` export: `<path or "not exported">` (committed if path is in-tree)
- Filter export `.pmf`: `<path or "not exported">`

## RESEARCH cross-references

- Filter recipe: 30-RESEARCH.md § "Filter recipe for ConPTY + restricted-token failures" (lines 232-243)
- Categories: 30-RESEARCH.md § "Events to look for" (lines 245-249)
- Sixth-option examples: 30-RESEARCH.md § "What 'surfaced a 6th option' looks like" (lines 252-256)
- Surfaced-nothing path: 30-RESEARCH.md § "What surfacing nothing looks like" (lines 259-263)

## Timebox

Wave 2 investigation is timeboxed to 3-5 working days per CONTEXT.md D-04. Day count tracking:
- Day 1: <date> — Task 1 + Task 2 (trace capture + analysis).
- Day 2: <date> — <activity>
- Day N: <date> — <terminal day; either implementation or exhaust>
```

If `*.pml` is not in `.gitignore`, add it now (`echo "*.pml" >> .gitignore` or via Edit tool). PML files are large binary artifacts that would bloat the repo.

**No FFI or production code edits in this task.** Pure documentation; produces the artifact Tasks 4-6 reference.
  </action>
  <acceptance_criteria>
- File `.planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md` exists.
- `grep -E "## Failure surface" .planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md` returns 1 match.
- `grep -E "## RESEARCH category match" .planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md` returns 1 match.
- `grep -E "## Trace evidence files" .planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md` returns 1 match.
- `grep -E "## Timebox" .planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md` returns 1 match.
- Either `.pml` binary file is referenced or noted as not exported; CSV export status documented.
- `grep -E "^\*\.pml$" .gitignore` returns 1 match (PML files gitignored).
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$f = '.planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md'; $g = '.gitignore'; if ((Test-Path $f) -and ((Select-String -Path $f -Pattern '## Failure surface' -Encoding utf8).Count -ge 1) -and ((Select-String -Path $g -Pattern '\*\.pml' -Encoding utf8).Count -ge 1)) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>30-WAVE-2-PROCMON.md captures Task 2 findings. Hypothesis + sixth-option sections present but empty (Task 4 fills). PML files gitignored.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Synthesize sixth-option proposal OR call exhaust-without-fix</name>
  <decision>
    Based on Task 3's documented evidence, propose a sixth option (a fix that does NOT mutate the token shape) or declare the investigation exhausted within the 3-5 working day timebox.
  </decision>
  <context>
    Per CONTEXT.md D-04 + RESEARCH § "What 'surfaced a 6th option' looks like" (lines 252-256), candidate fix shapes are:

    - **Per-session named-object DACL ACE:** Add a Low-IL-admit ACE to `\BaseNamedObjects\<X>` or `\Sessions\<n>\BaseNamedObjects\`. Likely an admin-install requirement (the per-user MSI may need a separate elevation step OR use of a privileged service).
    - **Sequencing fix:** Create the pseudoconsole BEFORE dropping integrity. Refactor `pty_proxy::open_pty()` callers in `crates/nono-cli/src/exec_strategy_windows/launch.rs:1178-1276` so the HPCON allocation happens ahead of the token-cascade decision.
    - **Broker-process pattern:** Pre-load CLR/System32 DLLs into the supervisor (Medium-IL); inject by hand into the child Low-IL process. Microsoft Q&A's recommended workaround.

    Per RESEARCH § "What surfacing nothing looks like" (lines 259-263), if no candidate emerges within the timebox: ship the failure-mode outcome.

    The decision drives Tasks 5 vs 6:
    - **propose-sixth-option:** Task 5 implements; Task 6 ships success-path bookkeeping.
    - **exhaust-without-fix:** Task 5 SKIPPED; Task 6 ships failure-path bookkeeping (cookbook revert + SHELL-01 → ✘ deferred).
  </context>
  <options>
    <option id="propose-named-object-dacl">
      <name>Sixth option: Per-session named-object DACL edit</name>
      <pros>Scoped to a setup-time helper; mirrors `crates/nono-cli/src/setup.rs` patterns. Preserves the Low-IL token shape (no cascade refactor).</pros>
      <cons>Likely needs admin elevation (one-time per Windows session OR per machine). Documented admin-install requirement may impact POC user experience.</cons>
    </option>
    <option id="propose-sequencing-fix">
      <name>Sixth option: Pseudoconsole-before-token sequencing</name>
      <pros>Keeps the per-user MSI deployment shape; no admin requirement. Refactor is contained in `launch.rs` PTY branch.</pros>
      <cons>Touches the PROC_THREAD_ATTRIBUTE_PSEUDOCONSOLE attribute-list construction; risk of regressing existing detached path. Requires Plan 30-03 harness re-run for full verification.</cons>
    </option>
    <option id="propose-broker-process">
      <name>Sixth option: Broker-process pattern (pre-load + inject)</name>
      <pros>Microsoft-documented workaround for ConPTY+IL-mismatch (Pitfall 2). Strong upstream signal that this works.</pros>
      <cons>Significant new code surface (broker module). May exceed the 3-5 working day timebox by itself. Carries v3.0 risk: broker is its own attack surface.</cons>
    </option>
    <option id="propose-other">
      <name>Sixth option: Something the trace surfaced that none of the canonical examples cover</name>
      <pros>Goal of Wave 2 is to surface a NEW option; if the trace did, this is the win.</pros>
      <cons>By construction, plan-time analysis can't anticipate this. Document specifically what was surfaced; cite trace evidence in 30-WAVE-2-PROCMON.md.</cons>
    </option>
    <option id="exhaust-without-fix">
      <name>Exhaust without fix — ship Phase 30 as v3.0 / kernel-driver deferral</name>
      <pros>Honest accounting of timebox; preserves planning integrity; cookbook reverts cleanly per RESEARCH Option Rev-B.</pros>
      <cons>POC users get `nono run -- claude` (non-TUI) until v3.0; cookbook needs to set expectations precisely.</cons>
    </option>
  </options>
  <resume-signal>Type one of: `propose-named-object-dacl`, `propose-sequencing-fix`, `propose-broker-process`, `propose-other`, `exhaust-without-fix`. If `propose-other`, also paste a one-paragraph description of the surfaced option.</resume-signal>
  <files>(checkpoint task — no file modifications by Claude; operator runs harness on Windows test box and reports results)</files>
  <action>This is a checkpoint task. The operator follows the steps in &lt;how-to-verify&gt; (or &lt;decision&gt; / &lt;options&gt; for decision checkpoints) and types the appropriate &lt;resume-signal&gt; phrase to resume the plan. The plan branches on the resume-signal value. No autonomous Claude edits in this task.</action>
  <verify>
    <automated>echo "checkpoint:human task — verify by operator-typed resume-signal value matching one of the documented options"</automated>
  </verify>
  <done>Operator has typed a recognized resume-signal value; downstream tasks branch accordingly.</done>
</task>

<task type="auto">
  <name>Task 5: [PROPOSE-SIXTH-OPTION PATHS ONLY] Implement and verify the sixth option</name>
  <files>crates/nono-cli/src/supervised_runtime.rs, crates/nono-cli/src/exec_strategy_windows/launch.rs (potentially), crates/nono-cli/src/setup.rs (potentially), .planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md</files>
  <read_first>
    - .planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md (the Hypothesis section + Sixth-option proposal — both filled by Task 4)
    - .planning/phases/30-windows-nono-shell-architecture/30-RESEARCH.md § "What 'surfaced a 6th option' looks like" (the proposal-shape examples)
    - .planning/phases/30-windows-nono-shell-architecture/30-PATTERNS.md lines 563-575 (Wave 2 supervised_runtime.rs hypothetical-fix pattern)
    - The specific source files implicated by Task 4's chosen option:
      - For `propose-named-object-dacl`: `crates/nono-cli/src/setup.rs` (existing per-user setup helpers; analog for the new admin-install step).
      - For `propose-sequencing-fix`: `crates/nono-cli/src/exec_strategy_windows/launch.rs:1178-1276` (the PTY branch in spawn_windows_child).
      - For `propose-broker-process`: novel module; no analog. Read the existing supervisor code to understand the IPC shape.
      - For `propose-other`: as appropriate.
  </read_first>
  <action>
**EXECUTE THIS TASK ONLY IF Task 4 = `propose-*` (any sixth-option path).** Skip if `exhaust-without-fix`.

Implementation specifics depend on the chosen option. Plan-time can't pre-bind to an option choice. The executor of THIS task receives the option from Task 4's resume signal and must:

1. **Update 30-WAVE-2-PROCMON.md's "Sixth-option proposal" section** with:
   - Proposal name (matching the resume signal).
   - Code locations to modify (file:line ranges).
   - Acceptance shape (how Wave 1 harness verifies the fix).
   - Risk assessment (what existing tests / paths might regress).

2. **Implement the option.** General principles:
   - Preserve CLAUDE.md security non-negotiables: no `.unwrap()`/`.expect()` in production code, all errors via `NonoError`, validate paths via component comparison, fail-secure on any unsupported shape.
   - For `propose-named-object-dacl` — likely a new function in `setup.rs` that calls Win32 `SetSecurityInfo` on the named-object handle. Wrap in `unsafe { ... }` with `// SAFETY:` comment per CLAUDE.md.
   - For `propose-sequencing-fix` — refactor the order of HPCON allocation vs token construction in `launch.rs::spawn_windows_child`. Be careful to preserve OwnedHandle discipline (Pitfall 1 / 5).
   - For `propose-broker-process` — exceeds typical task budget; consider splitting into a sub-plan if the implementation exceeds ~30% context.

3. **Add unit tests for the new code.** Mirror existing patterns:
   - For DACL helpers: pattern from `crates/nono/src/sandbox/windows.rs` `try_set_mandatory_label` tests.
   - For sequencing changes: extend `pty_token_gate_tests` (Plan 30-02 Task 2) with the new ordering invariant.
   - For broker-process: new test module with the broker-IPC contract.

4. **Re-run the Wave 1 harness** to verify the fix:
   ```
   pwsh -File scripts/test-windows-shell-write-deny.ps1
   pwsh -File scripts/test-windows-shell-tui.ps1
   ```
   Both must close cleanly (exit 0 / all PASS).

5. **Update 30-WAVE-2-PROCMON.md** with verification evidence:
   - Re-run date and harness exit codes.
   - Whether ProcMon trace at the failure surface is now clean (no Result != SUCCESS).

This task may need to be split if the implementation exceeds ~30% context. If so, create `.planning/phases/30-windows-nono-shell-architecture/30-05a-PLAN.md` (sub-plan) and return a `## CHECKPOINT REACHED — Task 5 split` signal to the orchestrator.

**Build clean check:**
```
cargo build -p nono-cli --target x86_64-pc-windows-msvc
cargo test -p nono-cli --target x86_64-pc-windows-msvc
cargo clippy -p nono-cli --target x86_64-pc-windows-msvc -- -D warnings -D clippy::unwrap_used
```
All exit 0.
  </action>
  <acceptance_criteria>
- `cargo build -p nono-cli --target x86_64-pc-windows-msvc` exits 0.
- `cargo test -p nono-cli --target x86_64-pc-windows-msvc` exits 0; new test(s) pass.
- `cargo clippy -p nono-cli --target x86_64-pc-windows-msvc -- -D warnings -D clippy::unwrap_used` exits 0.
- `pwsh -File scripts/test-windows-shell-write-deny.ps1` exits 0 (all acceptance PASS) on Windows test box.
- `pwsh -File scripts/test-windows-shell-tui.ps1` exits 0 on Windows test box.
- `grep -E "## Sixth-option proposal" .planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md` returns 1 match AND the section under it is no longer empty (operator-filled with the implemented option's narrative).
- `grep -E "## Verification" .planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md` returns 1 match (new section recording re-run evidence).
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p nono-cli --target x86_64-pc-windows-msvc && cargo test -p nono-cli --target x86_64-pc-windows-msvc</automated>
  </verify>
  <done>Sixth option implemented + tested + verified via Wave 1 harness re-run. 30-WAVE-2-PROCMON.md updated with the implemented option + verification evidence.</done>
</task>

<task type="auto">
  <name>Task 6: Final bookkeeping flip — success outcome OR v3.0 deferral</name>
  <files>.planning/PROJECT.md, .planning/STATE.md, .planning/debug/nono-shell-status-dll-init-failed.md, .planning/debug/resolved/nono-shell-status-dll-init-failed.md, .planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md, docs/cli/development/windows-poc-handoff.mdx</files>
  <read_first>
    - The Task 4 decision + Task 5 outcome (if executed)
    - .planning/phases/30-windows-nono-shell-architecture/30-RESEARCH.md § "Cookbook Rollback Path" (lines 323-364) — Option Rev-B specifics for the failure path
    - .planning/phases/30-windows-nono-shell-architecture/30-PATTERNS.md lines 374-417, 421-472 (PROJECT.md + STATE.md + cookbook patterns; both success and failure path cells)
    - Plan 30-04's intermediate state: PROJECT.md SHELL-01 row currently `⚠ ... Wave 2 ProcMon investigation in flight (Plan 30-05, ...)`
  </read_first>
  <action>
This task BRANCHES on Task 4's decision and Task 5's outcome. Two paths:

***

### SUCCESS PATH (Task 4 = `propose-*`, Task 5 = clean re-run)

This is structurally the same outcome as Plan 30-04's success path, except the cascade carries Wave 2's sixth-option implementation. The bookkeeping shape is identical to Plan 30-04 Task 5 SUCCESS PATH section, with these adjustments:

**Edit 1 — PROJECT.md SHELL-01 row:**
```
- ✔ **SHELL-01** — `nono shell` interactive ConPTY on Windows 10 17763+ via Low-IL primary token + Wave 2 <option-name> (Phase 30 D-01 + D-04); per-session WFP SID waived (AppID fallback, parallel to Phase 15 detached path); mandatory-label NO_WRITE_UP enforces write-deny outside grant set — v2.X Phase 30
```

(Replace `<option-name>` with the Wave 2 fix shorthand: e.g., "named-object DACL admit", "pseudoconsole-before-token sequencing", "broker process pattern".)

**Edit 2 — STATE.md Key Decisions (v2.3) Phase 30 entry:**

Replace the Wave 2 trigger entry from Plan 30-04 with a final-state entry that includes:
- Wave 1 cascade arm (per Plan 30-04 success-path entry text).
- Wave 2 trigger event + the Acceptance # that failed.
- Wave 2 surfaced-option name + implementation summary.
- Wave 2 verification evidence (harness re-run dates + acceptance PASS results).
- Updated structural grep invariants (e.g., for named-object DACL: `grep -c "<dacl-helper-fn-name>" crates/nono-cli/src/setup.rs` returns 1).
- Commits: 30-01..30-04 + 30-05's commit hash.

**Edit 3 — Debug session move + final flip:**

Same as Plan 30-04 Task 5 SUCCESS PATH Edit 4, with:
- `resolved_by: phase-30-plan-05` (this plan).
- Resolution section narrative includes Wave 2 trigger + sixth-option fix + harness verification.

**Edit 4 — Cookbook security-envelope paragraph:**

Same as Plan 30-04 Task 4 (the success-path text), with adjustments to mention the Wave 2 sixth option:
- For `named-object DACL` — note that initial setup needs admin elevation; document the one-time setup step.
- For `sequencing fix` — no operator-facing impact; cookbook text matches Plan 30-04 Task 4 verbatim.
- For `broker process` — note the supervisor-side broker; performance / resource impact if any.

**Edit 5 — STATE.md `stopped_at`:**
`stopped_at: Phase 30 complete (Wave 2 success path; <option-name> implemented; SHELL-01 → ✔ validated)`

**Edit 6 — 30-WAVE-2-PROCMON.md final state:**

Append a section:
```markdown
## Final outcome

**Result:** Wave 2 SUCCESS — sixth option implemented; Wave 1 harness re-run on <DATE> closed all acceptance criteria.
**Implemented option:** <option-name>
**Phase 30 ships:** Yes — SHELL-01 → ✔ validated.
**Cookbook updated:** Yes — security-envelope paragraph added.
**Debug session:** moved to `.planning/debug/resolved/nono-shell-status-dll-init-failed.md`.
**Commits:** <hash 30-05>.
```

***

### FAILURE PATH (Task 4 = `exhaust-without-fix` OR Task 5 didn't close)

**Edit 1 — PROJECT.md SHELL-01 row:**
```
- ✘ **SHELL-01** — `nono shell` on Windows is structurally incompatible with simultaneous WRITE_RESTRICTED + ConPTY + Low-IL primary token at user-mode (Phase 30 evidence; ProcMon trace surfaced <failure-surface>); deferred to v3.0 kernel mini-filter driver work
```

(Replace `<failure-surface>` with the ProcMon-surfaced category from Task 2: e.g., "ConPTY ALPC integrity-mismatch", "BaseNamedObjects per-session DACL exclusion", "ImageLoad chain DllMain failure".)

**Edit 2 — STATE.md Key Decisions (v2.3) Phase 30 entry — failure-mode shape:**

```markdown
- **Phase 30 Wave 1 + Wave 2 — `nono shell` on Windows deferred to v3.0:** Wave 1 cascade arm landed (`WindowsTokenArm::LowIlPrimary` in `launch.rs::spawn_windows_child`); unit tests pass. Field smoke on <DATE> failed Acceptance #<N> (<launch | tui-render | write-deny>) per `30-FIELD-SMOKE.md` operator log. Wave 2 ProcMon investigation (3-5 working days) surfaced <failure-surface> per `30-WAVE-2-PROCMON.md`. <Concise summary of what was tried and why it didn't close.> Phase 30 ships failure-mode finding: `nono shell` on Windows is structurally incompatible with simultaneous WRITE_RESTRICTED + ConPTY + Low-IL primary token at user-mode (the four failure modes — 0xC0000142 launch, integrity-mismatch input drop, capability-pipe DACL exclusion, mandatory-label evasion — span the user-mode token shape space). Cookbook (`docs/cli/development/windows-poc-handoff.mdx`) reverted per RESEARCH Option Rev-B: stripped the `<Note>` recommendation, Step 4 instruction, Step 5 interactive-verification block, and Step 6 row; KEPT the "Known limitation" section. Added "nono shell on Windows is deferred to v3.0" section. POC users get `nono run -- claude` (non-TUI) until v3.0 kernel mini-filter driver work lands. SHELL-01 PROJECT.md row → ✘ deferred to v3.0. Debug session moved to `.planning/debug/resolved/` with failure-mode Resolution section. Wave 1 cascade arm code stays in tree — it works for the launch step (Acceptance #1) on its own; the integrity-related downstream acceptance criteria are what defer. The unit tests + helper enum stay as guards on the underlying mechanism for whenever v3.0 kernel-driver work activates this path. Commits: 30-01..30-05.
```

**Edit 3 — Debug session move + final failure-mode resolution:**

Move file to `.planning/debug/resolved/nono-shell-status-dll-init-failed.md`. Update frontmatter:
- `status: resolved`
- `resolved_by: phase-30-plan-05`
- `head_commit: <hash 30-05>`
- `related_phase: 30-windows-nono-shell-architecture`

Append a `## Resolution` section:
```markdown
## Resolution

**Resolved by:** Phase 30 Plan 30-05 — Wave 2 exhausted without surfacing a workable user-mode option.

**Outcome:** `nono shell --profile claude-code --allow-cwd` on Windows 10/11 is structurally incompatible with simultaneous WRITE_RESTRICTED + ConPTY + Low-IL primary token at user-mode. The ProcMon investigation surfaced <failure-surface> at the Win32 layer; no user-mode fix preserves all four phase acceptance criteria simultaneously.

**Phase 30 ships failure-mode finding:**
- SHELL-01 PROJECT.md row → ✘ deferred to v3.0.
- Cookbook reverted per RESEARCH §"Cookbook Rollback Path" Option Rev-B; "Known limitation" section retained; "nono shell on Windows is deferred to v3.0" section added.
- POC users get `nono run -- claude` (non-TUI) until v3.0 kernel mini-filter driver work lands.

**Cross-references:**
- Phase 30 CONTEXT: `.planning/phases/30-windows-nono-shell-architecture/30-CONTEXT.md`
- Phase 30 RESEARCH: `.planning/phases/30-windows-nono-shell-architecture/30-RESEARCH.md`
- Phase 30 Wave 1 field smoke: `.planning/phases/30-windows-nono-shell-architecture/30-FIELD-SMOKE.md`
- Phase 30 Wave 2 ProcMon evidence: `.planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md`

**v3.0 kernel mini-filter driver scope:**
The deferred Phase 6b territory (long-deferred to v3.0) is what will close `nono shell` on Windows with full TUI + write-deny. CONTEXT.md `<deferred>` block enumerates the shape; this Resolution doesn't pre-bind to a v3.0 architecture.
```

**Edit 4 — Cookbook revert per Option Rev-B:**

Per RESEARCH §"Wave 2 failure path (cookbook revert)" (lines 358-364) — Option Rev-B is text replacement, NOT git revert:

In `docs/cli/development/windows-poc-handoff.mdx`:

1. **STRIP** the top-of-doc `<Note>` block content that recommends `nono shell` (lines 10-17). Replace with a more limited Note:
   ```markdown
   <Note>
   `nono run -- <command>` is the supported invocation for the POC on Windows. Long-form interactive shells (`nono shell`) are deferred to v3.0 — see "Known limitation" below for context.
   </Note>
   ```

2. **STRIP** the Step 4 instruction "Use `nono shell`, not `nono run`" (lines 155-160) and replace with:
   ```markdown
   For the POC, use `nono run -- <command>` for non-TUI commands. TUI agents (claude, codex, etc.) cannot host inside the sandbox on the current Windows binary — see "Known limitation" below.
   ```

3. **STRIP** the Step 5 "Interactive verification (manual)" block (lines 217-231).

4. **STRIP** the Step 6 row "Always use `nono shell` on Windows" (line 250) and the related rows 249, 252, 253 (anything mentioning `nono shell`). Replace with rows that recommend `nono run` only, OR simply delete and rely on the "Known limitation" + Step 4 update.

5. **KEEP** the "Known limitation" section (lines 233-239) — RESEARCH explicitly notes this content is factually correct and stays.

6. **ADD** a new section after "Known limitation" titled "`nono shell` on Windows is deferred to v3.0":
   ```markdown
   ## `nono shell` on Windows is deferred to v3.0

   Phase 30 (May 2026) attempted to land OS-enforced filesystem write protection AND interactive TUI rendering for `nono shell --profile <name>` on Windows 10/11. The investigation surfaced that no user-mode token shape can deliver both simultaneously:

   - WRITE_RESTRICTED + ConPTY → `STATUS_DLL_INIT_FAILED (0xC0000142)` at child launch.
   - Low-IL primary token + ConPTY → <Wave 2 surfaced failure: ALPC integrity-mismatch / named-object DACL / etc.>
   - Null token → no OS-level write enforcement.
   - Anonymous-pipe stdio → no TUI rendering.

   The path forward is a v3.0 kernel mini-filter driver (long-deferred Phase 6b territory). For the POC: use `nono run -- claude` (non-TUI) on Windows.

   See `.planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md` for the technical evidence.
   ```

**Edit 5 — STATE.md `stopped_at`:**
`stopped_at: Phase 30 deferred to v3.0 (Wave 2 exhausted; SHELL-01 → ✘; cookbook reverted per RESEARCH Option Rev-B)`

**Edit 6 — 30-WAVE-2-PROCMON.md final state:**

Append:
```markdown
## Final outcome

**Result:** Wave 2 EXHAUSTED — no workable user-mode option surfaced within the 3-5 working day timebox.
**Investigation activities:** <list of attempts: e.g., "Day 1: trace capture + analysis", "Day 2: tested named-object DACL hypothesis — denied at higher kernel layer", "Day 3: reviewed broker-process feasibility — exceeded budget", ...>
**Phase 30 ships:** failure-mode finding; SHELL-01 → ✘ deferred to v3.0.
**Cookbook reverted:** Option Rev-B (text replacement); "Known limitation" retained; new "deferred to v3.0" section added.
**Debug session:** moved to `.planning/debug/resolved/nono-shell-status-dll-init-failed.md` with failure-mode Resolution section.
**v3.0 follow-up:** kernel mini-filter driver work (Phase 6b territory; long-deferred).
**Commits:** <hash 30-05>.
```

***

### COMMON to both paths

After all edits, run:
- `cargo test -p nono-cli` — must pass.
- `make ci` — full verification.
- Single git commit with DCO sign-off.
  </action>
  <acceptance_criteria>

**SUCCESS PATH:**
- `grep -E "✔ \*\*SHELL-01\*\*.*Wave 2" .planning/PROJECT.md` returns 1 match.
- `grep -E "✘ \*\*SHELL-01\*\*" .planning/PROJECT.md` returns 0 matches.
- `grep -E "Wave 2 success" .planning/STATE.md` returns at least 1 match.
- File `.planning/debug/resolved/nono-shell-status-dll-init-failed.md` exists with `## Resolution` section.
- File `.planning/debug/nono-shell-status-dll-init-failed.md` does NOT exist.
- `grep -E "## Final outcome" .planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md` returns 1 match with "SUCCESS" outcome.

**FAILURE PATH:**
- `grep -E "✘ \*\*SHELL-01\*\*.*deferred to v3\.0" .planning/PROJECT.md` returns 1 match.
- `grep -E "✔ \*\*SHELL-01\*\*" .planning/PROJECT.md` returns 0 matches.
- `grep -E "deferred to v3\.0" .planning/STATE.md` returns at least 1 match.
- File `.planning/debug/resolved/nono-shell-status-dll-init-failed.md` exists with failure-mode Resolution section.
- `grep -E "## \`nono shell\` on Windows is deferred to v3\.0" docs/cli/development/windows-poc-handoff.mdx` returns 1 match.
- `grep -cE "Use \`nono shell\`, not \`nono run\`" docs/cli/development/windows-poc-handoff.mdx` returns 0 (instruction stripped per Option Rev-B).
- `grep -cE "Known limitation: \`nono run\` cannot host TUI agents" docs/cli/development/windows-poc-handoff.mdx` returns 1 (KEPT per Option Rev-B).
- `grep -E "## Final outcome" .planning/phases/30-windows-nono-shell-architecture/30-WAVE-2-PROCMON.md` returns 1 match with "EXHAUSTED" outcome.

**COMMON:**
- `cargo test -p nono-cli` exits 0 (no production-code regression introduced by Wave 2 — even on failure path, Wave 1's cascade arm + tests stay in tree).
- `make ci` exits 0.
- Single commit with DCO sign-off.
- `grep -c "Phase 30" .planning/STATE.md` ≥ 2.
- `grep -c "Phase 30" .planning/PROJECT.md` ≥ 1.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nono-cli && powershell -NoProfile -Command "$ok = (Test-Path .planning/debug/resolved/nono-shell-status-dll-init-failed.md) -and -not (Test-Path .planning/debug/nono-shell-status-dll-init-failed.md); if ($ok) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>Phase 30 reaches its terminal state. PROJECT.md, STATE.md, debug session, cookbook all coherent with the Wave 2 outcome (success OR exhaust). 30-WAVE-2-PROCMON.md captures the full investigation trail. Single commit with DCO sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProcMon trace data ↔ analysis conclusions | Trace data is high-fidelity but interpreting "the proximate failure" requires judgment. Misidentification triggers wrong fix attempts. |
| Wave 2 implementation ↔ Wave 1 invariants | Sixth-option implementation must NOT regress Wave 1 cascade arm + unit tests. The truth-table tests from Plan 30-02 are the guard. |
| Cookbook revert ↔ POC user expectations | If Wave 2 exhausts and cookbook is NOT reverted, POC users follow stale advice and hit production failures. RESEARCH Option Rev-B is mandatory on the failure path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-22 | Tampering | Wave 2 sixth-option implementation regresses existing Wave 1 cascade arm or its unit tests | mitigate | Task 5 acceptance criteria includes `cargo test -p nono-cli` exit 0 — every existing test passes including Plan 30-02's `pty_token_gate_tests` and `low_integrity_primary_token_tests`. |
| T-30-23 | Information disclosure | Cookbook on failure path retains `nono shell` recommendation; POC users get false security claim | mitigate | Task 6 FAILURE PATH acceptance criteria: `grep -cE "Use \`nono shell\`, not \`nono run\`"` returns 0. The RESEARCH Option Rev-B text replacement is mandatory. |
| T-30-24 | DoS / scope creep | Wave 2 implementation exceeds 3-5 working day timebox without converging | mitigate | Task 4 has explicit `exhaust-without-fix` option; D-04 timebox is enforced by checkpoint discipline. Task 6 ships either path. |
| T-30-25 | Repudiation | Wave 2 evidence (ProcMon trace) lost / not committed; future readers can't audit the deferral decision | mitigate | Task 3 commits `30-WAVE-2-PROCMON.md` with trace-file references + filter recipe + failure surface. PML binary is gitignored but CSV export OR detailed text notes are committed. |
| T-30-26 | Tampering | Cookbook failure-path revert misses one of the recommendation locations (top-of-doc Note OR Step 4 OR Step 5 OR Step 6 row) | mitigate | Task 6 FAILURE PATH acceptance criteria has explicit grep gates for stripped + retained text. Each removal location is a separate gate. |
</threat_model>

<verification>
- ProcMon trace captured + analyzed within 3-5 working day timebox.
- Either: sixth option implemented + Wave 1 harness re-run closes acceptance, OR exhaust-without-fix declared and v3.0 deferral shipped.
- All bookkeeping artifacts (PROJECT.md, STATE.md, debug session, cookbook, 30-WAVE-2-PROCMON.md) reach their terminal state.
- `cargo test -p nono-cli` + `make ci` exit 0.
- Single commit with DCO sign-off.
</verification>

<success_criteria>
- D-04 satisfied: Wave 2 ran, was timeboxed, produced one of the two terminal outcomes (success OR exhaust).
- D-07 honored: cookbook content is honest (success: documents Wave 2 fix; failure: reverts and points at v3.0 deferral).
- D-10 closed terminally: SHELL-01 → ✔ validated v2.X Phase 30 OR ✘ deferred to v3.0. No middle state.
- 30-WAVE-2-PROCMON.md is the single canonical Wave 2 investigation record.
- POC users get truthful guidance from the cookbook on either path.
</success_criteria>

<output>
After completion, create `.planning/phases/30-windows-nono-shell-architecture/30-05-SUMMARY.md` per the template at @$HOME/.claude/get-shit-done/templates/summary.md. Note explicitly:
- Task 4's resume signal (chosen sixth-option name OR `exhaust-without-fix`).
- Task 5 outcome if executed.
- Task 6 SUCCESS or FAILURE path taken.
- Total Wave 2 duration in working days.
- ProcMon evidence file paths.
- Whether Phase 30 ships in v2.3 or slips to v2.4 per CONTEXT D-07.
- Final commit hash.
- Any v3.0 follow-up scope notes (kernel mini-filter driver).
</output>
