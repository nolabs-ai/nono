---
phase: 36.5
slug: profile-drafts-feature-absorption-optional
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-13
---

# Phase 36.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (Rust 1.77 builtin) |
| **Config file** | none — Cargo.toml at workspace root |
| **Quick run command** | `cargo test --workspace --lib --bins profile_drafts -- --nocapture` |
| **Full suite command** | `cargo test --workspace --all-features` |
| **Estimated runtime** | ~120 seconds (quick) / ~600 seconds (full, Windows host) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test --workspace --lib --bins -- --test-threads=1` (full unit suite, ~60s on Windows host)
- **After every plan wave:** Run `cargo test --workspace --all-features`
- **Before `/gsd-verify-work`:** Full suite + `cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used` (Windows host AND `--target x86_64-unknown-linux-gnu` AND `--target x86_64-apple-darwin`)
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

> Filled by gsd-planner. Each task in 36.5-01-DRAFTS-ABSORPTION-PLAN.md must have one row.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 36.5-01-XX | 01 | N | REQ-PORT-CLOSURE-03 | T-36.5-XX / — | {filled by planner} | unit / integration | `cargo test {pattern}` | ✅ / ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/nono-cli/tests/profile_drafts_test.rs` — integration test module for `init --draft`, `validate --draft`, `promote`, `--yes`, base-hash mismatch, shadow-refusal
- [ ] `crates/nono-cli/src/profile_cmd.rs` — `#[cfg(test)]` module for `cmd_promote` unit cases (hash compute, diff render path selection)
- [ ] `crates/nono-cli/src/package_status.rs` — `#[cfg(test)]` module for `PackageStatusResponse` serde round-trip and registry-client mock

*If existing infrastructure already covers a row, plan a coverage check rather than a new file.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Interactive `[y/N]` diff prompt rendering | REQ-PORT-CLOSURE-03 #2 | Stdin TTY behavior not reliably automatable cross-platform; `--yes` covers the non-interactive auto path | 1. `nono profile init --draft mybot`; 2. Edit `profile-drafts/mybot.json`; 3. `nono profile promote mybot`; 4. Confirm diff renders + prompt appears; 5. Press `n` → expect "aborted" notice |
| Cross-volume `fs::rename` atomicity on Windows (if a draft lives on a different volume than the canonical store) | REQ-PORT-CLOSURE-03 #4 | Requires multi-volume Windows host; out of CI scope | 1. On a Windows test box with separate drives, place `%APPDATA%\nono\profile-drafts\` on a different volume; 2. `nono profile promote <name>`; 3. Expect either success (same-NTFS-volume) or graceful failure with a clear error (cross-volume) |
| Phase 15 5-row detached-console smoke gate | (close-gate row #6) | Phase 36.5 does not touch exec_strategy / detached-console surface — expected documented-skipped | Documented-skipped per D-36.5-C4 enumeration row 6 |
| `wfp_port_integration` test suite | (close-gate row #7) | Phase 36.5 does not touch WFP surface — expected documented-skipped | Documented-skipped per D-36.5-C4 enumeration row 7 |
| `learn_windows_integration` test suite | (close-gate row #8) | Phase 36.5 does not touch learn surface — expected documented-skipped | Documented-skipped per D-36.5-C4 enumeration row 8 |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] Cross-target clippy (Linux + macOS) wired into execute-phase task list
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
