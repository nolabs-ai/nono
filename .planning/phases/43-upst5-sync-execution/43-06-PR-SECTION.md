## Plan 43-06 — Cluster 4 Windows platform detection (registry queries + REG_DWORD fix)

**Cluster:** 4 (Windows platform detection — 2 upstream commits as a unit: `0748cced` robust Windows registry parsing + `5d821c12` REG_DWORD parsing fix)
**Disposition:** `fork-preserve` (W-8 canonical value; Task 1 D-43-C1 verdict 2026-05-18; foundation-constraint-forced by Plan 43-05's `fork-preserve` per 43-PATTERNS.md replay-when-foundation-is-also-replayed pattern + trial-pick clause-(a) corroboration: 2 content conflicts on `wiring.rs` + `schema.json`)
**Upstream commits:** `0748cced` (feat Windows registry detection) + `5d821c12` (REG_DWORD parsing fix). Landed as a unit per Phase 42 ledger Cluster 4 rationale via single combined D-20 replay commit (Phase 40 Plan 40-06 SUMMARY DEC-2 + Phase 43 Plan 43-05 SUMMARY DEC-4 fold-into-single-commit precedent) — no partial-landing path possible.
**Files touched:** `crates/nono-cli/src/platform.rs` (Windows-branch swap `WindowsInfo::default()` → `detect_windows()` + 4 new factory functions `detect_windows`/`detect_windows_version`/`query_windows_registry_value`/`parse_windows_registry_value` with REG_DWORD hex→decimal + REG_DWORD unit test; `When::parse` visibility tightened; `When::deserialize` single-string fast-path; `matches_windows` build_version `map_or("", \|part\| part)` panic-safety; `compare_versions` non-numeric mismatch ordering = `Less`; `version_segments_compare_numerically_when_possible` assertion update); `crates/nono-cli/src/profile/mod.rs` (`deserialize_conditional_string_vec` unknown-sibling-field rejection; `SecretsConfig` Deserialize rewrite to `serde_json::Value`-based form with explicit unknown-key rejection; `conditional_profile_entries_reject_unknown_fields` integration test); `crates/nono-cli/data/nono-profile.schema.json` (WhenPredicate description text extended with `linux:fedora:>=43:workstation` example).
**Key decision:** D-43-C1 diff-inspection authority applied; `resolved_disposition = fork-preserve` CONSTRAINED by Plan 43-05's `fork-preserve` per 43-PATTERNS.md § Plan 43-06 replay-when-foundation-is-also-replayed pattern. D-43-E1 4-condition addendum applied per-hunk (full table in 43-06-DISPOSITION-RESOLUTION.md): all Windows-specific code lives INSIDE `platform.rs` (cross-platform module) dispatched by `cfg!(target_os = "windows")` runtime check — NOT in fork's `*_windows.rs` files. Windows-fallback decision: **Option A — uniform behavior, upstream wins** (audit: zero fork-side divergent registry-detection path exists; single `restricted_token.rs:84` comment about "registry traversal" describes kernel-side token access, unrelated). REG_DWORD panic-safety preserved via `map_or("", \|part\| part)` per 5d821c12. Phase 36-01b From-impl exhaustive enumeration + Phase 36-01c `bypass_protection` rename both UNTOUCHED. W-5 + W-7 fixes NOT NEEDED for Branch B (single combined replay commit; no partial-landing path possible). W-8 fix: canonical `fork-preserve` value used throughout (PLAN.md + SUMMARY frontmatter + this PR section).
**Cargo gates (Windows host):** `cargo test --workspace --all-features` 2208 passed / 0 failed / 19 ignored (+2 new tests: `windows_registry_dword_values_are_decimalized` + `conditional_profile_entries_reject_unknown_fields`); `cargo clippy --workspace --all-targets -- -D warnings -D clippy::unwrap_used` clean (no rust-1.95 lint regression vector — `map_or("", \|part\| part)` form is lint-clean); `cargo fmt --all -- --check` clean.
**Phase 43 terminal-plan close:** Plan 43-06 is the Wave 2b terminal plan per D-43-A3. All 6 plan SUMMARYs present after this plan closes (43-01..43-06). Cluster 6 macOS-lint won't-sync inline documentation + ROADMAP/STATE updates + UPST6 hand-off are downstream orchestrator scope per D-43-D1 / D-43-D2.
**CI baseline diff:** zero `success → failure` transitions vs baseline `13cc0628` expected; orchestrator fills in `43-06-CLOSE-GATE.md` § "Lane transitions" post-merge.
