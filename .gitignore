#Windows stuff

dist/windows/*.msi
dist/windows/*.wixpdb

/target
.vscode/
.idea/
.DS_Store

# Claude state and config
.claude/

# misc
proj/
build_notes/
docs/presentations/
docs/cli/development/


# Library tests
bindings/python/.venv*
bindings/node/node_modules*

# Gemini CLI planning state

# GSD session scratch — per-session bookmarks and local workflow config.
# Not project state; generated and managed by GSD tooling at runtime.
.continue-here.md
**/.continue-here.md
.planning/config.json
.planning/HANDOFF.json

# WFP service debug crumbs (19-CLEAN-03 D-11)
host.nono_binary.commit
/query

# Plan-execution scratch directory (per-plan staging of upstream files for manual port)
.tmp/

# Local CI / drift-audit scratch (Phase 33 D-33-A2 — raw drift JSON is regenerable, not committed)
ci-logs-local/

# Sysinternals ProcMon traces (Phase 30 Plan 30-05) — large binaries, evidence is in CSV/MD
*.pml

# PoC build artifacts (quick-task 260508-m99 broker-process PoC; user builds locally on Windows)
.planning/quick/*/poc-broker/Cargo.lock
.planning/quick/*/poc-broker/target/

