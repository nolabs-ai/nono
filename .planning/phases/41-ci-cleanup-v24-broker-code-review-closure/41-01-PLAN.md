---
phase: 41-ci-cleanup-v24-broker-code-review-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nono-cli/src/exec_strategy.rs
autonomous: true
requirements:
  - REQ-CI-01
must_haves:
  truths:
    - "Per D-01: Plan 41-01 lands FIRST so Phase 37 (parallel) can rebase on this API migration."
    - "Per D-02: This plan addresses the `CapabilityRequest::path` → `HandleTarget::FilePath` migration class (14 call sites in `crates/nono-cli/src/exec_strategy.rs`)."
    - "Per D-04: A single-site spike at line 2662 succeeds and lints clean before the remaining 13 sites are bulk-migrated."
    - "Per D-06: `cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` exits 0 from the Windows host after migration."
    - "REQ-CI-01: zero `#[deprecated]` warnings remain for `CapabilityRequest::path` reads in `exec_strategy.rs` on Linux/macOS clippy."
  artifacts:
    - path: "crates/nono-cli/src/exec_strategy.rs"
      provides: "Private `request_path(request: &CapabilityRequest) -> &Path` helper + 14 migrated call sites + 0 deprecated-field reads in non-helper code."
      contains: "fn request_path"
  key_links:
    - from: "crates/nono-cli/src/exec_strategy.rs (14 call sites)"
      to: "fn request_path()"
      via: "private module-internal helper call"
      pattern: "request_path\\(&request\\)"
    - from: "fn request_path()"
      to: "nono::HandleTarget::FilePath"
      via: "match on request.target"
      pattern: "HandleTarget::FilePath \\{ path \\}"
---

<objective>
Migrate the 14 deprecated `nono::CapabilityRequest::path` field reads inside `crates/nono-cli/src/exec_strategy.rs::handle_capability_message` to use the new `HandleTarget::FilePath` shape via a single private `request_path()` helper. Localize the deprecation surface so future Phase 11 wire-shape retirement touches one helper, not 14 call sites.

Purpose: This plan lands FIRST (per D-01) so Phase 37 (parallel) can rebase on the migrated API surface. Closes 14 of the 33 Linux/macOS clippy errors enumerated in `.planning/PHASE-41-TRACKER.md`.

Output: Updated `crates/nono-cli/src/exec_strategy.rs` with `fn request_path()` defined once and called at every site that previously read `request.path`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-ci-cleanup-v24-broker-code-review-closure/41-CONTEXT.md
@.planning/phases/41-ci-cleanup-v24-broker-code-review-closure/41-RESEARCH.md
@.planning/phases/41-ci-cleanup-v24-broker-code-review-closure/41-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from RESEARCH.md verified scan of crates/nono/src/supervisor/types.rs:101-196. -->
<!-- Executor should use these directly — no codebase exploration needed for the migration target shape. -->

From `crates/nono/src/supervisor/types.rs` (verified line ranges):
```rust
// HandleTarget enum (lines 101-145) — #[serde(tag = "type")] tagged enum with 6 variants
pub enum HandleTarget {
    FilePath { path: PathBuf },
    // ... other variants
}

// CapabilityRequest struct (lines 153-196)
#[allow(deprecated)]  // line 151 — struct itself reads its own deprecated field silently
pub struct CapabilityRequest {
    // Phase 11 deprecated field — still serialized for wire-compat
    #[deprecated(note = "use HandleTarget::FilePath via the new kind/target fields")]
    pub path: PathBuf,  // lines 163-164

    // Phase 18 AIPC-01 new shape
    #[serde(default)]
    pub kind: HandleKind,  // line 184 — defaults to File

    #[serde(default)]
    pub target: Option<HandleTarget>,  // line 189 — defaults to None for Phase-11-shaped requests

    pub access: AccessMode,  // pre-existing
}
```

The migration shape: the deprecation warning fires at read sites in `exec_strategy.rs` (not inside the struct definition due to line 151's `#[allow(deprecated)]`). The fix is a read-pattern change at the consumer sites; the wire format is unchanged.

Helper signature to ADD (private to `exec_strategy.rs`):
```rust
fn request_path(request: &nono::CapabilityRequest) -> &std::path::Path;
```

The 14 call sites and their three shape categories are enumerated in `41-PATTERNS.md` § `crates/nono-cli/src/exec_strategy.rs` — Plan 41-01 + Plan 41-02 and 41-RESEARCH.md § Plan 41-01.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supervisor ↔ Sandboxed child (AIPC IPC) | `CapabilityRequest` crosses this boundary; the path field carries an attacker-influenceable PathBuf that drives capability decisions. The migration MUST preserve the same semantic value at every consumer site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | T (Tampering) | `request_path()` helper returns a `&Path` that flows into 14 capability-decision sites. If the helper returns a different path than the legacy `request.path` read, capability decisions could change subtly. | mitigate | Helper falls back to `&request.path` when `target` is `None` (Phase 11 wire shape); spike at site 2662 verifies semantic equivalence via clippy + tests before bulk apply. |
| T-41-01-02 | I (Information Disclosure) | Bulk replacing 14 sites without verifying each path-type coercion could leak `&PathBuf` vs `&Path` shape changes into `DenialRecord` audit entries. | accept | Compiler enforces type via `request_path(...).to_path_buf()` at DenialRecord struct-literal sites; spike validates compile + clippy clean. LOW severity — no externally-observable security surface change in this CI-hygiene plan. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: SPIKE — Add `request_path()` helper and migrate site at exec_strategy.rs:2662 only; verify clippy + tests pass</name>
  <files>crates/nono-cli/src/exec_strategy.rs</files>
  <read_first>
    - crates/nono-cli/src/exec_strategy.rs (read the imports section at the top, then read lines 2650-2820 to understand the full `handle_capability_message` function context AND see all 14 sites in one pass)
    - crates/nono/src/supervisor/types.rs (read lines 101-196 to confirm `HandleTarget::FilePath` and `CapabilityRequest` field shapes)
    - .planning/phases/41-ci-cleanup-v24-broker-code-review-closure/41-RESEARCH.md § Plan 41-01 (research-led migration brief)
    - .planning/phases/41-ci-cleanup-v24-broker-code-review-closure/41-PATTERNS.md (Pattern 1: CapabilityRequest path-extraction helper)
  </read_first>
  <action>
    Per D-04 (spike protocol), perform the API migration at exactly ONE site first to validate the pattern compiles, lints clean, and preserves semantics.

    Step 1 — Add the helper. At a suitable private-module-scope location in `crates/nono-cli/src/exec_strategy.rs` (RESEARCH suggests above `handle_capability_message` or at the top of the same scope; planner discretion permits picking the exact placement during the read), add this exact function (copied from 41-PATTERNS.md Pattern 1, verified against `crates/nono/src/supervisor/types.rs:101-196`):

    ```rust
    // Reads the request's path using the AIPC-01 shape (target = Some(FilePath{path}))
    // with fallback to the Phase 11 deprecated `path` field. Localizes the
    // deprecation surface to ONE place: once Phase 11 wire-shape is retired,
    // only this helper changes.
    fn request_path(request: &nono::CapabilityRequest) -> &std::path::Path {
        use nono::HandleTarget;
        match &request.target {
            Some(HandleTarget::FilePath { path }) => path.as_path(),
            _ => {
                #[allow(deprecated)]
                { &request.path }
            }
        }
    }
    ```

    If `nono::HandleTarget` is not already imported at the top of the file, the `use nono::HandleTarget;` inside the match expression is intentional and scoped — do NOT add a top-level import.

    Step 2 — Migrate ONLY the site at line 2662 (a Shape 1 owned-PathBuf-into-DenialRecord site per 41-PATTERNS.md). The exact transform is:
    ```rust
    // BEFORE (line 2662 area):
    DenialRecord { path: request.path.clone(), /* other fields */ }
    // AFTER:
    DenialRecord { path: request_path(&request).to_path_buf(), /* other fields */ }
    ```

    Leave the other 13 sites unchanged for this task.

    Step 3 — Run the cross-target clippy verification standard locally before committing:
    ```
    cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used
    cargo clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used
    ```
    (macOS cross-target may fall back to CI runner if the local toolchain is unavailable; Linux is non-negotiable per memory `feedback_clippy_cross_target`.)

    Expected outcome: clippy reports the count of remaining `CapabilityRequest::path` deprecation warnings decreased by exactly 1 (from baseline to baseline-1) and zero new warnings or errors appeared elsewhere. If the count did not decrease by exactly 1, or if clippy flagged a new warning, STOP and re-inspect the helper before proceeding to Task 2.

    Commit the helper + one-site migration as `chore(41-01): spike CapabilityRequest::path migration helper at site 2662` with the standard DCO sign-off. Per `crates/nono-cli/build.rs` regenerating bindings as a side effect is OK; do NOT include unrelated diffs in the commit.

    **Reasoning embedded:** Per D-04 spike-then-bulk pattern (Pitfall 2 in RESEARCH explicitly warns against bulk-applying without a spike — a type mismatch surfacing at site 11/14 would require backing out 10 sites of churn). Per D-06 cross-target clippy is mandatory because Windows-host workspace clippy cannot see `#[cfg(target_os = "linux")]` blocks.
  </action>
  <verify>
    <automated>cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used 2>&1 | grep -c "CapabilityRequest::path.*deprecated" | tee /tmp/41-01-spike-count.txt</automated>
  </verify>
  <acceptance_criteria>
    - `fn request_path` exists exactly once in `crates/nono-cli/src/exec_strategy.rs`. Verify with: `grep -c '^fn request_path\|^\s*fn request_path' crates/nono-cli/src/exec_strategy.rs` returns 1.
    - The site formerly reading `request.path.clone()` at approximately line 2662 now reads `request_path(&request).to_path_buf()`. Verify with: `grep -n 'request_path(&request).to_path_buf()' crates/nono-cli/src/exec_strategy.rs` returns at least one line in the 2660-2700 range.
    - The 13 remaining migration sites (lines 2684, 2690, 2696, 2705, 2710, 2717, 2729, 2742, 2757, 2763, 2781, 2794, 2809) still contain `request.path` reads — this is INTENTIONAL for Task 1; they migrate in Task 2.
    - Cross-target Linux clippy from Windows host completes successfully (the count file `/tmp/41-01-spike-count.txt` shows the deprecation count decreased by exactly 1 vs the pre-task baseline; if you want a strict zero-deprecations check, that belongs in Task 2's acceptance criteria).
    - Commit message starts with `chore(41-01): spike` and includes `Signed-off-by:` per CLAUDE.md DCO requirement.
  </acceptance_criteria>
  <done>Helper added; site 2662 migrated; cross-target Linux clippy passes; commit signed-off-by; clippy deprecation count for `CapabilityRequest::path` decreased by exactly 1.</done>
</task>

<task type="auto">
  <name>Task 2: Bulk-apply migration to the remaining 13 call sites and run final cross-target clippy verification</name>
  <files>crates/nono-cli/src/exec_strategy.rs</files>
  <read_first>
    - crates/nono-cli/src/exec_strategy.rs (re-read lines 2650-2820 once if the file changed materially during Task 1; otherwise skip the re-read — the spike's structure is already in your context window)
    - .planning/phases/41-ci-cleanup-v24-broker-code-review-closure/41-PATTERNS.md § `crates/nono-cli/src/exec_strategy.rs` for the three shape-mapping templates
  </read_first>
  <action>
    Bulk-migrate the remaining 13 sites using the three shape templates verified in 41-PATTERNS.md (lines 62-77). Use grep first to enumerate every remaining `request.path` reference inside the function:

    ```bash
    grep -n 'request\.path' crates/nono-cli/src/exec_strategy.rs
    ```

    Expected sites (verified from TRACKER + RESEARCH): 2684, 2690, 2696, 2705, 2710, 2717, 2729, 2742, 2757, 2763, 2781, 2794, 2809. If grep returns additional sites, migrate those too — RESEARCH § Plan 41-01 notes the `open_path_for_access` call at lines 2808-2814 may be one extra site not in the original tracker count.

    Apply the three shape templates from 41-PATTERNS.md (lines 62-77):

    - **Shape 1 — owned PathBuf into DenialRecord (sites 2696, 2729, 2742, 2763, 2781, 2794):**
      `DenialRecord { path: request.path.clone(), ... }` → `DenialRecord { path: request_path(&request).to_path_buf(), ... }`

    - **Shape 2 — display in format!/debug!/warn! (sites 2684, 2690, 2710, 2717, 2757):**
      `request.path.display()` → `request_path(&request).display()`

    - **Shape 3 — &Path/&PathBuf parameter pass (sites 2684, 2705, 2710, 2809 and any open_path_for_access call site):**
      `overlapping_protected_root(&request.path, ...)` → `overlapping_protected_root(request_path(&request), ...)`
      `ti.check_path(&request.path)` → `ti.check_path(request_path(&request))`
      `open_path_for_access(&request.path, &request.access, ...)` → `open_path_for_access(request_path(&request), &request.access, ...)`

    **Risk per RESEARCH § Plan 41-01 Risk areas:**
    - The `DenialRecord::path` field is `PathBuf`, not `&Path`. The helper returns `&Path`. Always use `.to_path_buf()` for owned writes (Shape 1).
    - For function call sites accepting `&PathBuf` (rare), a `&Path` from the helper deref-coerces in the OPPOSITE direction; if a callee insists on `&PathBuf`, wrap: `&request_path(&request).to_path_buf()`. Prefer fixing the callee signature to `&Path` instead.

    After all sites are migrated, run the full cross-target clippy gate from the Windows host:
    ```
    cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used
    cargo clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used
    ```

    Both invocations MUST exit 0 with zero deprecation warnings for `CapabilityRequest::path`. Verify with: `cargo clippy --workspace --target x86_64-unknown-linux-gnu --message-format=json 2>&1 | grep -c "CapabilityRequest::path.*deprecated"` returns `0`.

    Run `cargo test -p nono-cli --lib 2>&1 | tail -20` to verify no unit tests broke. Run `cargo build --workspace --target x86_64-unknown-linux-gnu` to confirm the migration compiles on the Linux target.

    Commit as `chore(41-01): migrate 13 remaining CapabilityRequest::path call sites to request_path() helper` with the DCO sign-off and a body listing the migrated line numbers per shape category.

    **Reasoning embedded:** This task is mechanical replacement once the spike (Task 1) proved the helper compiles and lints clean. Bulk-apply is safe because Task 1's spike already validated the type-coercion shape for Shape 1 (DenialRecord path field). Per D-06 the cross-target clippy from Windows host is the verification standard.
  </action>
  <verify>
    <automated>cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used 2>&1 | grep -v '^#' | grep -c "CapabilityRequest::path.*deprecated"</automated>
  </verify>
  <acceptance_criteria>
    - Zero deprecation warnings for `CapabilityRequest::path` remain on the Linux target. Verify with: `cargo clippy --workspace --target x86_64-unknown-linux-gnu --message-format=json 2>&1 | grep -c "CapabilityRequest::path.*deprecated"` returns `0`.
    - Zero direct `request.path` reads remain outside `fn request_path` itself. Verify with: `grep -v 'fn request_path\|#\[allow(deprecated)\]\|& *request\.path *}' crates/nono-cli/src/exec_strategy.rs | grep -c 'request\.path' | tee /tmp/41-01-residual-count.txt` returns `0` (the `grep -v` excludes the helper's own internal fallback line).
    - `request_path(&request)` appears at least 14 times in the file. Verify with: `grep -c 'request_path(&request)' crates/nono-cli/src/exec_strategy.rs` returns `>= 14`.
    - macOS-target clippy passes from CI (or locally if cross-toolchain available). Verify with: GitHub Actions macOS Clippy lane goes green on the resulting commit (manual close-gate check, not strictly automated).
    - `cargo test -p nono-cli --lib` exits 0 (no broken unit tests).
    - `cargo build --workspace --target x86_64-unknown-linux-gnu` exits 0.
    - Commit message contains `chore(41-01):` and `Signed-off-by:`.
  </acceptance_criteria>
  <done>All 14 (or more, if grep finds extras) `request.path` reads migrated; clippy Linux green with zero `CapabilityRequest::path` deprecation warnings; unit tests pass; commit signed-off-by.</done>
</task>

</tasks>

<verification>
- `cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used` exits 0 with zero `CapabilityRequest::path.*deprecated` lines.
- `cargo clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used` exits 0 (CI runner if no local cross-toolchain).
- `cargo build --workspace --target x86_64-unknown-linux-gnu` exits 0.
- `cargo test -p nono-cli --lib` exits 0.
- The 14 (or more) call sites identified in 41-PATTERNS.md all read via `request_path(&request)` not `request.path`.
- Phase 37 can rebase on this commit cleanly (manual verification post-merge).
</verification>

<success_criteria>
- 14 of 33 Linux/macOS clippy errors from `.planning/PHASE-41-TRACKER.md` are eliminated.
- Single `request_path()` helper is the only place inside the file that reads `CapabilityRequest::path` directly (with `#[allow(deprecated)]` scoped to the fallback expression).
- D-01 (Plan 41-01 lands first) honored: this plan is Wave 1 with empty `depends_on`.
- D-04 (spike-then-bulk) honored: Task 1 migrates one site, Task 2 migrates the rest.
- D-06 (cross-target clippy) honored: both Linux and macOS clippy gates referenced in acceptance criteria.
</success_criteria>

<output>
After completion, create `.planning/phases/41-ci-cleanup-v24-broker-code-review-closure/41-01-SUMMARY.md` documenting:
- The exact set of sites migrated (line numbers as found post-migration).
- Helper placement (file:line).
- Cross-target clippy commands run and their exit codes.
- Confirmation that Phase 37 can now rebase on the post-migration `exec_strategy.rs`.
</output>
