---
phase: 27.1-nono-test-home-seam
verified: 2026-05-04T00:00:00Z
status: human_needed
score: 5/6 must-haves verified (1 partial — D-27.1-14 contingency invoked)
overrides_applied: 1
overrides:
  - must_have: "Phase 27 redesigned Test 1 body (preserved under #[ignore]) runs to completion on Windows host with NONO_TEST_HOME set."
    reason: "Plan 03 success criterion #7 + ROADMAP D-27.1-14 explicitly authorize the large-fix-branch outcome: tests re-#[ignore]'d when Blocker 3 surfaces architectural gaps beyond a missed-callsite small fix. Two genuine v2.4 production-code follow-ups (audit-loader swap, bundle-target architecture) were surfaced and are documented in deferred-items.md. The seam itself reaches the audit-attestation flow end-to-end (verified by the `nono run --audit-integrity` step on Windows host writing to <NONO_TEST_HOME>/.nono/audit/<id>/), which is the seam-side acceptance criterion for REQ-NTH-03 partial closure."
    accepted_by: "Phase 27.1 Plan 03 Task 3 checkpoint (D-27.1-14 contingency)"
    accepted_at: "2026-05-04T00:00:00Z"
gaps: []
human_verification:
  - test: "Windows-host audit-attestation tests with v2.4-FU-1 + v2.4-FU-2 production fixes applied"
    expected: "`cargo test -p nono-cli --test audit_attestation` returns `2 passed; 0 failed; 0 ignored` once v2.4-FU-1 (audit-loader swap in audit_commands.rs) and v2.4-FU-2 (bundle-target architecture decision) land. Currently `2 ignored` is the explicitly-allowed D-27.1-14 contingency outcome."
    why_human: "Requires v2.4 production code to be written (audit_commands.rs loader swap + sign-target architecture decision). Not a Phase 27.1 deliverable per D-27.1-14: 'Do NOT block the seam landing on Blocker 3.'"
  - test: "Confirm WR-01 split-brain home resolution is acceptable for production binaries"
    expected: "Either (a) WR-01 is accepted as documented intentional design (NONO_TEST_HOME and validated_home() resolve to different sources by design — production users do not set NONO_TEST_HOME), or (b) a follow-up phase is opened to unify the two resolvers per code-review WR-01 fix recommendation."
    why_human: "WR-01 from 27.1-REVIEW.md identifies a confused-deputy hazard: validated_home() (security-sensitive, HOME-chain) and nono_home_dir() (test-seam, NONO_TEST_HOME-honoring) can resolve to different paths under partial misconfiguration in always-on release binaries (D-27.1-08). Plan 01 SUMMARY T-27.1-05 disposition documents this as 'intentional'; the threat is real but the disposition was an intentional design call. A human needs to confirm the disposition stands, or open a follow-up to unify resolvers."
---

# Phase 27.1: nono-test-home-seam Verification Report

**Phase Goal:** Add `NONO_TEST_HOME` env-var override to `dirs::home_dir()` callsites in `crates/nono-cli/src/` so Windows integration tests can redirect HOME without LOCALAPPDATA/USERPROFILE drift. Unblocks REQ-AAH-01 (Phase 27) and queued v2.3 plans for Windows-host execution.
**Verified:** 2026-05-04T00:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (merged from ROADMAP success criteria + plan must_haves)

| #   | Truth | Status     | Evidence       |
| --- | ----- | ---------- | -------------- |
| 1   | `dirs::home_dir()` callsites in `crates/nono-cli/src/` honor `NONO_TEST_HOME` when set, fall through to platform default otherwise (ROADMAP SC #1) | VERIFIED | All 15 plan-enumerated callsites route through `crate::config::nono_home_dir()` (verified by grep: 0 non-comment occurrences of `dirs::home_dir` or `xdg_home::home_dir` outside config/mod.rs's own helper body and tests). Helper at `crates/nono-cli/src/config/mod.rs:120-133` honors NONO_TEST_HOME first, else falls through to `dirs::home_dir().ok_or(NonoError::HomeNotFound)`. |
| 2   | Phase 27 redesigned Test 1 body (preserved under `#[ignore]`) runs to completion on Windows host with `NONO_TEST_HOME` set (ROADMAP SC #2) | PASSED (override) | Test bodies adapted (run_nono threads NONO_TEST_HOME via Command::env at audit_attestation.rs:25; only_audit_session_id used at line 372; cross-platform run_command_args() used at line 499). However both tests are CURRENTLY `#[ignore]`'d (lines 320 and 489) per D-27.1-14 large-fix branch — the seam reaches the supervisor (verified by `nono run --audit-integrity` writing to `<NONO_TEST_HOME>/.nono/audit/<id>/` on Windows), but two distinct downstream production-code gaps (v2.4-FU-1 audit-loader, v2.4-FU-2 bundle-target) surfaced. Seam-side acceptance criterion satisfied; full test green is deferred to v2.4 follow-ups (out of milestone scope). See override entry. |
| 3   | No production-path behavior change when `NONO_TEST_HOME` is unset (security-equivalent to status quo) (ROADMAP SC #3) | VERIFIED | Helper code path when env unset (config/mod.rs:131-132) is identical to pre-Phase-27.1 behavior: `dirs::home_dir().ok_or(NonoError::HomeNotFound)`. user_state_dir() unset path (lines 178-180) is byte-identical pre/post: `dirs::state_dir().or_else(dirs::data_local_dir).map(|p| p.join("nono"))`. session.rs:410 deviation preserves legacy ConfigParse error string. `crates/nono/` is byte-identical (D-19 confirmed: `git diff --stat 18e8e4ea..HEAD -- crates/nono/` is empty). |
| 4   | First use of the override emits exactly one `tracing::warn!` per process containing the resolved path (REQ-NTH-01 acceptance #3) | VERIFIED | `warn_once_test_home` at config/mod.rs:135-143 uses `std::sync::OnceLock<()>` guard. Note: IN-02 in REVIEW correctly identifies a small race window — comment at line 140 acknowledges "Worst case: warn fires twice across racing threads." Bounded by N (small thread count). Plan 01 SUMMARY's claim "exactly one" is technically "at-most-once-steady-state, at-most-N-on-first-call" — informational only, no security impact. |
| 5   | `crates/nono/` remains byte-identical (D-19 invariant — explicit cross-cutting constraint) | VERIFIED | `git diff --stat 18e8e4ea..HEAD -- crates/nono/` returns empty output (zero modifications across all 3 plans). `crates/nono::NonoError::EnvVarValidation` and `crates/nono::NonoError::HomeNotFound` already existed; no new error variants needed. |
| 6   | xdg-home dependency removed from `crates/nono-cli/Cargo.toml` (REQ-NTH-01 acceptance #6) | VERIFIED | `grep -c '^xdg-home' crates/nono-cli/Cargo.toml` returns 0; `grep 'xdg-home' Cargo.lock` returns 0 hits. Pre-flight grep (Plan 02 SUMMARY) confirmed no other crate consumed it. |

**Score:** 5/6 verified + 1 PASSED (override) for ROADMAP SC #2

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `crates/nono-cli/src/config/mod.rs` | `pub fn nono_home_dir() -> Result<PathBuf>` honoring NONO_TEST_HOME | VERIFIED | Function at lines 120-133, signature exact. Validates absolute paths, fail-closes on relative with EnvVarValidation, falls through to dirs::home_dir().ok_or(HomeNotFound) when unset. |
| `crates/nono-cli/src/config/mod.rs` | `pub fn user_state_dir() -> Option<PathBuf>` extended with NONO_TEST_HOME branch | VERIFIED | Function at lines 164-181 honors NONO_TEST_HOME (returns Some(<path>/.nono)) when set+absolute; preserves Option<PathBuf> signature; falls through to platform default when unset. |
| `crates/nono-cli/src/config/mod.rs` | OnceLock-guarded warn_once_test_home helper | VERIFIED | Function at lines 135-143 emits `tracing::warn!("NONO_TEST_HOME override active: {path}")` guarded by `static WARNED: std::sync::OnceLock<()>`. |
| `crates/nono-cli/src/audit_session.rs:36` | audit_root() routes through nono_home_dir() | VERIFIED | `let home = crate::config::nono_home_dir()?;` followed by `.join(".nono").join("audit")`. |
| `crates/nono-cli/src/rollback_session.rs:40` | rollback_root() Unix arm routes through nono_home_dir() | VERIFIED | `let home = crate::config::nono_home_dir()?;` in `#[cfg(not(target_os = "windows"))]` arm. Windows arm uses user_state_dir() (REQ-NTH-02). |
| `crates/nono-cli/src/protected_paths.rs:43` | from_defaults() Unix arm routes through nono_home_dir() | VERIFIED | `let home = crate::config::nono_home_dir()?;` in `#[cfg(not(target_os = "windows"))]` arm. |
| `crates/nono-cli/src/rollback_runtime.rs:202` | match arm routes through nono_home_dir() | VERIFIED | `let home = crate::config::nono_home_dir()?;` |
| `crates/nono-cli/src/hooks.rs:73` | install_claude_code_hook() routes through nono_home_dir() | VERIFIED | `let home = crate::config::nono_home_dir()?;` |
| `crates/nono-cli/src/package_cmd.rs:175,1012,1015` | unregister_claude_code_hook + expand_tilde route through nono_home_dir() | VERIFIED | All 3 callsites confirmed; tail-position site at line 1015 returns `crate::config::nono_home_dir()` directly. |
| `crates/nono-cli/src/profile/mod.rs:1981` | home_dir() wrapper delegates to nono_home_dir() | VERIFIED | Wrapper body replaced with `crate::config::nono_home_dir()` (line 1981); ~30 internal callers stay untouched. |
| `crates/nono-cli/src/session.rs:410` | sessions_dir() routes through nono_home_dir() with map_err preserving ConfigParse | VERIFIED | `let home = crate::config::nono_home_dir().map_err(|_| { NonoError::ConfigParse(...) })?;` Plan 02 documented deviation; legacy contextual error message preserved. |
| `crates/nono-cli/src/audit_commands.rs:725` | shorten_home() routes through nono_home_dir() (display) | VERIFIED | `if let Ok(home) = crate::config::nono_home_dir() { ... }` (Plan 02 deviated from spec's `.ok()` form to clippy-clean `if let Ok` form — semantically identical). WR-02 from REVIEW documents pre-existing string-prefix anti-pattern (CLAUDE.md Common Footgun #1) inside this function — pre-existing; the migration touched the resolver line only. |
| `crates/nono-cli/src/rollback_commands.rs:245,1522` | shorten_home() + test route through nono_home_dir() (display) | VERIFIED | Both sites use `if let Ok(home) = crate::config::nono_home_dir()`. Same WR-02 string-prefix pre-existing pattern in shorten_home; the test (line 1522) is best-effort against actual home dir. |
| `crates/nono-cli/src/trust_scan.rs:482,486` | expand_home() routes through nono_home_dir() (display) | VERIFIED | Both arms use `if let Ok(home) = crate::config::nono_home_dir()`. |
| `crates/nono-cli/Cargo.toml` | xdg-home dependency removed | VERIFIED | `grep -c '^xdg-home' crates/nono-cli/Cargo.toml` returns 0; Cargo.lock has no entry. |
| `crates/nono-cli/tests/audit_attestation.rs` | run_nono threads NONO_TEST_HOME; tests adapted to seam | VERIFIED (with override on test execution) | Lines 12-27: run_nono passes NONO_TEST_HOME via Command::env. Test 1 simplified to `only_audit_session_id(&home)` at line 372. Test 2 cross-platform via run_command_args() at line 499. setup_isolated_home pre-creates `<home>/.nono/{audit,rollbacks}` at lines 74-77. **Both tests currently `#[ignore]`'d** (lines 320, 489) per D-27.1-14 large-fix branch with descriptive Blocker-3-resurfaced notes. |

### Deferred Items

Items not yet met but explicitly tracked as v2.4 milestone follow-ups in `deferred-items.md` (NOT addressed in any later phase of the v2.3 milestone — Phases 28 + 29 are unrelated REQ areas).

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Test 1 (`audit_verify_reports_signed_attestation_with_pinned_public_key`) green on Windows host | v2.4 (FU-1: audit-loader swap) | `audit_commands.rs:12` imports `load_session` from `rollback_session`; audit-only sessions live at `<home>/.nono/audit/<id>/` and are unfindable. The audit-aware loader at `audit_session.rs:160` already exists but is `#[allow(dead_code)]`. Tracked in `deferred-items.md` § v2.4-FU-1 with 30-60 min estimate. |
| 2 | Test 2 (`rollback_signed_session_verifies_from_audit_dir_bundle`) green on Windows host | v2.4 (FU-2: bundle-target architecture) | `audit_attestation::sign_session_attestation` writes bundle to `session_dir = <rollback>/<id>/` when `rollback_active`; test asserts bundle at `<audit>/<id>/`. Architecture decision required (mirror, dual-root verify, or test rewrite). Tracked in `deferred-items.md` § v2.4-FU-2 with 1-2h estimate. **WR-04 from REVIEW additionally identifies that the test asserts a JSON shape (`json["attestation"]["present"]` etc.) that does not match production output (`attestation_present`/`attestation_valid`); v2.4-FU-3 estimate of 15 min is too low and needs revision.** |
| 3 | Re-enable both audit-attestation tests after FU-1+FU-2 land | v2.4 (FU-3) | 15 min estimated (low — see WR-04 caveat above); tracked in `deferred-items.md`. |

These items are **NOT actionable Phase 27.1 gaps** per ROADMAP D-27.1-14 ("Do NOT block the seam landing on Blocker 3") and Plan 03 success criterion #7 ("`cargo test ...` passes both tests OR the failure is handled per D-27.1-14 contingency"). They are next-milestone (v2.4) follow-up work.

### Key Link Verification

| From | To  | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `audit_session::audit_root` | `config::nono_home_dir` | direct call | WIRED | Line 36: `let home = crate::config::nono_home_dir()?;` followed by `.join(".nono").join("audit")`. Subprocess receiving NONO_TEST_HOME env var produces audit dir at `<NONO_TEST_HOME>/.nono/audit` (verified end-to-end by Plan 03 Task 3 Windows-host run — `nono run --audit-integrity` succeeded and wrote bundle there). |
| `rollback_session::rollback_root` (Windows arm) | `config::user_state_dir` | conditional cfg | WIRED | Windows arm preserves existing call to `crate::config::user_state_dir()`; user_state_dir extension routes to `<NONO_TEST_HOME>/.nono` per REQ-NTH-02. |
| `audit_attestation.rs::run_nono` (test) | supervisor's `nono_home_dir()` | `Command::env("NONO_TEST_HOME", home)` | WIRED | Line 25: `.env("NONO_TEST_HOME", home);`. Subprocess inherits the override; supervisor's `audit_root()` (production code) honors it. End-to-end verified: `nono run --audit-integrity` writes session at the expected NONO_TEST_HOME-rooted path on Windows host. |
| Helper warn-once | `tracing::warn!` | `OnceLock<()>` | WIRED | Line 138: `tracing::warn!("NONO_TEST_HOME override active: {}", path.display());` guarded by `WARNED.get().is_none()` + `WARNED.set(())`. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `nono_home_dir()` | path resolution | `std::env::var("NONO_TEST_HOME")` (primary) → `dirs::home_dir()` (fallthrough) | Yes | FLOWING |
| `user_state_dir()` | state path | `std::env::var("NONO_TEST_HOME")` (primary) → `dirs::state_dir()`/`dirs::data_local_dir()` (fallthrough) | Yes | FLOWING |
| `audit_root()` | audit dir path | `nono_home_dir()` + `.join(".nono").join("audit")` | Yes | FLOWING |
| `rollback_root()` Unix | rollback path | `nono_home_dir()` + `.join(".nono").join("rollbacks")` | Yes | FLOWING |
| `rollback_root()` Windows | rollback path | `user_state_dir()` + `.join("rollbacks")` | Yes | FLOWING |
| Test subprocess receives override | NONO_TEST_HOME env | `Command::env(..., home)` in run_nono helper | Yes | FLOWING (end-to-end on Windows host: supervisor wrote audit session to NONO_TEST_HOME-rooted path during Plan 03 Task 3 verification) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| nono-cli builds clean | `cargo build -p nono-cli` | Finished `dev` profile in 11.33s | PASS |
| nono_home_dir helper unit tests | `cargo test -p nono-cli --bin nono config::tests::nono_home_dir` | 3 passed; 0 failed; 0 ignored (cross-platform tests: returns_override_when_set, rejects_non_absolute_override, falls_through_when_unset) | PASS |
| user_state_dir Windows test | `cargo test -p nono-cli --bin nono config::tests::user_state_dir_honors_nono_test_home` | 1 passed; 0 failed (Windows-only test confirms override produces Some(<abs>\\.nono)) | PASS |
| audit_attestation integration tests | `cargo test -p nono-cli --test audit_attestation` | 0 passed; 0 failed; **2 ignored** (D-27.1-14 outcome with descriptive Blocker-3 notes) | PASS-with-contingency (per Plan 03 SC #7 and override entry above) |
| Workspace D-19 invariant | `git diff --stat 18e8e4ea..HEAD -- crates/nono/` | (empty) | PASS |
| xdg-home removed from Cargo.toml | `grep -c '^xdg-home' crates/nono-cli/Cargo.toml` | 0 | PASS |
| xdg-home removed from Cargo.lock | `grep 'xdg-home' Cargo.lock` | 0 hits | PASS |
| Workspace-wide invariant: no non-comment dirs::home_dir in nono-cli/src/ | `grep -rn 'dirs::home_dir' crates/nono-cli/src/` | Only matches inside `config/mod.rs` (helper's own fallthrough at line 132 and 4 doc-comments) and 1 test fall-through assertion (line 441) — all expected and correct. | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| REQ-NTH-01 | Plan 01 + Plan 02 | `nono_home_dir()` helper + 15-callsite migration + xdg-home removal | SATISFIED | Helper at config/mod.rs:120-133, 4 unit tests passing, all 15 callsites migrated (verified by grep), xdg-home gone from Cargo.toml/Cargo.lock, `crates/nono/` byte-identical. All 6 acceptance criteria from REQUIREMENTS.md satisfied. |
| REQ-NTH-02 | Plan 01 | user_state_dir() Windows redirection under NONO_TEST_HOME | SATISFIED | user_state_dir() at config/mod.rs:164-181 honors NONO_TEST_HOME (returns Some(<abs>/.nono)) on all platforms when set+absolute, preserves status-quo when unset, signature stays Option<PathBuf>. Windows-only unit test passing. All 4 acceptance criteria satisfied. |
| REQ-NTH-03 | Plan 03 | Phase 27 audit-attestation tests re-enabled and adapted | PARTIALLY SATISFIED | Tests adapted to seam (run_nono threads NONO_TEST_HOME; only_audit_session_id used; cross-platform run_command_args); seam reaches supervisor end-to-end on Windows host (verified). However both tests are currently `#[ignore]`'d per D-27.1-14 large-fix branch (Plan 03 SC #7 explicitly authorizes this outcome). Acceptance criteria #1 (#[ignore] removed) is NOT met as-of-now, but acceptance #5 (Blocker 3 contingency) explicitly authorizes the partial closure. v2.4-FU-1 + v2.4-FU-2 + v2.4-FU-3 close the test side. **Override applied per D-27.1-14.** |
| REQ-AAH-01 | Phase 27 → re-routed via Phase 27.1 | Re-enable fixture-driven attestation tests | PARTIALLY SATISFIED (by proxy) | Phase 27.1 seam landed and reaches Phase 27's deferred test path; Phase 27 redesigned Test 1 body preserved with seam-based simplification. Closure on Windows host requires v2.4-FU-1 + v2.4-FU-2. Roadmap row 230 records Phase 27 status as PARTIAL pending v2.4. Not a Phase 27.1 acceptance gap — REQUIREMENTS.md row 301 explicitly maps AAH-01 closure to "Status will flip to Complete when Plan 27.1-03 verification passes on Windows host (D-27.1-14 contingency may yield partial closure)." |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `crates/nono-cli/src/config/mod.rs` | 146, 163 | `#[allow(dead_code)]` on user_config_dir + user_state_dir | INFO | Pre-existing pattern (mirrors existing utility-function convention in the same file). Plan 01's helper attribute was intentionally removed by Plan 02 once consumers landed. |
| `crates/nono-cli/src/audit_commands.rs` | 723-732 | `shorten_home` uses `s.strip_prefix(&home_str)` (string-form prefix on path data) | WARNING (per WR-02) | CLAUDE.md § Common Footgun #1. Pre-existing pattern; Plan 02 only changed the resolver call (dirs::home_dir → nono_home_dir). Display-only impact: `~/Tester2/...` could mis-shorten. Not a goal-blocking gap; flagged in REVIEW for follow-up. |
| `crates/nono-cli/src/rollback_commands.rs` | 243-252 | Same string-prefix pattern as above | WARNING (per WR-02) | Same disposition. |
| `crates/nono-cli/src/config/mod.rs` | 120-133 | nono_home_dir accepts paths with `..` components (only checks `is_absolute()`) | INFO (per IN-01) | Downstream consumers (protected_paths::resolve_path, validate_path_within) canonicalize before filesystem ops. Forensic tracing::warn line emits raw un-canonicalized path. Per CLAUDE.md § Path Handling, hardening recommended but not a goal-blocking gap. |
| `crates/nono-cli/src/config/mod.rs` | 135-143 | warn_once_test_home race window (get-then-set instead of get_or_init) | INFO (per IN-02) | Bounded by N threads on first contended access; comment at line 140 acknowledges. SUMMARY claim "exactly once" technically inaccurate; not a security or correctness issue. |
| **MULTIPLE** | — | **WR-01 split-brain home resolution** (`validated_home()` HOME-chain vs `nono_home_dir()` NONO_TEST_HOME-honoring; production binaries honor both per D-27.1-08 always-on) | WARNING (per WR-01, REVIEW) | Confused-deputy class hazard: in production with `HOME=/home/victim NONO_TEST_HOME=/tmp/attacker`, `policy::sensitive_paths` expands `~/.ssh` against `/home/victim` while `audit_root()` writes to `/tmp/attacker/.nono/audit/`. Documented in Plan 01 SUMMARY T-27.1-05 as **intentional** disposition; threat is real but disposition was an explicit design call locked at planning time. Surfaced for human verification (see human_verification section). |
| `crates/nono-cli/tests/audit_attestation.rs` | 347-365 | Direct std::env::set_var/remove_var without RAII restore in test | WARNING (per WR-05, REVIEW) | If `run_nono` panics, env var leaks. Currently `#[ignore]`'d so no live CI impact; will become live when v2.4-FU-1+FU-2 land and the `#[ignore]` is removed. Recommend a local ScopedEnvVar RAII pattern for v2.4-FU-3. Not a Phase 27.1 acceptance gap. |
| `crates/nono-cli/tests/audit_attestation.rs` | 533-538 | Test 2 asserts JSON shape (`json["attestation"]["present"]` etc.) that does not match production output | WARNING (per WR-04, REVIEW) | v2.4-FU-3's "15 min" estimate is too low because Test 2 needs JSON-shape rework or production cmd_verify needs to emit a richer nested attestation object. Currently `#[ignore]`'d so no live CI impact. Should be added to deferred-items.md as a v2.4-FU-3 caveat. |

### Human Verification Required

#### 1. v2.4 Production Follow-Ups for REQ-NTH-03 Full Closure

**Test:** Run `cargo test -p nono-cli --test audit_attestation` on Windows host AFTER v2.4-FU-1 (audit-loader swap in `audit_commands.rs:12` from `rollback_session::load_session` to `audit_session::load_session`) AND v2.4-FU-2 (bundle-target architecture decision: mirror to audit_dir, dual-root verify, or test rewrite) AND v2.4-FU-3 (remove both `#[ignore]` attributes).
**Expected:** `2 passed; 0 failed; 0 ignored`. This closes REQ-NTH-03 fully and REQ-AAH-01 (Phase 27) by proxy.
**Why human:** Requires v2.4 production code that is explicitly out of Phase 27.1 scope per D-27.1-14 ("Do NOT block the seam landing on Blocker 3"). Not a Phase 27.1 acceptance gap.

#### 2. WR-01 Split-Brain Home Resolution Disposition

**Test:** Confirm whether the documented "intentional" disposition for the split-brain between `validated_home()` and `nono_home_dir()` (Plan 01 SUMMARY T-27.1-05) is acceptable for v2.3 production binaries, OR open a follow-up phase to unify the two resolvers.
**Expected:** Either (a) acceptance of the intentional design (production users do not set NONO_TEST_HOME; the seam is a test-only de-facto contract) OR (b) a follow-up phase that either (i) removes NONO_TEST_HOME from production binaries via cfg-gating (contradicts D-27.1-08), or (ii) makes `validated_home()` honor NONO_TEST_HOME, or (iii) makes `nono_home_dir()` cross-check NONO_TEST_HOME against the validated HOME chain.
**Why human:** WR-01 from `27.1-REVIEW.md` identifies a confused-deputy hazard with security-policy-relevant consequences (audit logs and sensitive-path overlap rules can target distinct home directories within the same nono invocation). Plan 01 SUMMARY explicitly disposes of this as "intentional" via T-27.1-05. The disposition was an architectural decision at Phase 27.1 planning time, locked in CONTEXT D-27.1-08 ("always-on production seam"). The threat is real (see scenario at REVIEW WR-01 lines 60-78) but the design call was explicit. A human needs to confirm the disposition stands, or open a v2.3-or-v2.4 follow-up phase. Not a Phase 27.1 acceptance gap (the phase plan locked this disposition).

### Gaps Summary

**Status: human_needed.** No FAILED truths and no MISSING/STUB artifacts. All ROADMAP success criteria for Phase 27.1 are either VERIFIED or PASSED-via-override (with explicit, plan-locked authorization for the override).

The phase delivers the seam end-to-end:
- Helper `nono_home_dir()` exists, validates correctly, fail-closes on relative input, falls through cleanly when unset.
- All 15 plan-enumerated callsites migrated; xdg-home dependency removed.
- `crates/nono/` byte-identical (D-19 invariant satisfied).
- Unit tests pass (4 dedicated seam tests + 3 ran cross-platform during verification).
- Seam reaches the supervisor end-to-end on Windows host (verified in Plan 03 Task 3 by `nono run --audit-integrity` writing to `<NONO_TEST_HOME>/.nono/audit/<id>/`).

The 2 audit-attestation integration tests are **currently `#[ignore]`'d** per the explicit D-27.1-14 large-fix-branch contingency (Plan 03 SC #7, ROADMAP language "Do NOT block the seam landing on Blocker 3"). Two genuine v2.4 production-code follow-ups (audit-loader swap, bundle-target architecture) were surfaced and are documented in `deferred-items.md`. These are next-milestone (v2.4) backlog, NOT Phase 27.1 acceptance gaps.

**Two items require human disposition:**

1. The full test-green outcome for REQ-NTH-03 requires v2.4-FU-1 + v2.4-FU-2 + v2.4-FU-3 production code that is explicitly out of Phase 27.1 scope. The verifier seeks human confirmation that partial closure is the accepted outcome for v2.3 (per D-27.1-14).

2. The WR-01 split-brain home resolution warning from `27.1-REVIEW.md` documents a real confused-deputy hazard in always-on release binaries. The Plan 01 SUMMARY T-27.1-05 disposition treats this as "intentional" — the verifier seeks human confirmation that this disposition stands, or that a follow-up phase should unify the resolvers per the WR-01 fix recommendations.

Both items are surfaced for human decision rather than blocked as gaps because:
- The Phase 27.1 plan documents D-27.1-14 as an explicitly-allowed outcome.
- The WR-01 disposition was a locked architectural decision at planning time (D-27.1-08).
- All Phase 27.1 acceptance criteria are met or have plan-authorized overrides.

**Recommendation:** Accept Phase 27.1 as Complete with the understanding that:
(a) REQ-NTH-03 closes partially with v2.4-FU-* tracking the full test-green path (already reflected in REQUIREMENTS.md row 304 and ROADMAP.md row 231 "Complete").
(b) WR-01 / WR-02 / WR-04 / WR-05 / IN-01..04 are tracked as code-review follow-ups (a `crates/nono-cli/`-wide cleanup task is suggested in `deferred-items.md`).

---

_Verified: 2026-05-04T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
