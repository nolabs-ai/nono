## Plan 43-03 — Cluster 1 pack management (nono update + pinning/outdated + hints)

**Cluster:** 1 (Pack management — new `nono update` + `nono package pinning` + `nono package outdated` CLI surface + inline pack-update hints)
**Disposition:** will-sync (D-19 cherry-pick chain of 8 upstream SHAs: `64d9f283`, `a5985edd`, `be23d6df`, `5098fc10`, `317c97b7`, `18b03fa6`, `98c18f1f`, `42601ed7` — applied in upstream chronological order on worktree branch `worktree-agent-a7fcb300371d56aaf` serving as `43-03-cluster-1`)
**Upstream commits:** 8 cherry-picks (+ 6 SEPARATE `fix(43-03-cra):` follow-on commits per CLAUDE.md commit policy, never --amend; documented in SUMMARY § CR-A class regressions)
**Files touched:** crates/nono-cli/src/{app_runtime.rs, cli.rs, cli_bootstrap.rs, main.rs, pack_update_hint.rs (NEW), package.rs, package_cmd.rs, profile/mod.rs, registry_client.rs, sandbox_prepare.rs}
**Key decision:** Chronological-order discipline per Phase 40 Plan 40-01 DEV-1 lesson. Interim close-gate checkpoints at commits 3 and 5 (CR-A class regression handling per Phase 40 DEV-3). Phase 36-01b `From<ProfileDeserialize>` exhaustive match preserved (0 touches to the protected impl; the +73-line `list_pack_store_profiles` adapter at the bottom of `profile/mod.rs` is additive-only). Phase 36-01c `bypass_protection` rename honored (0 `override_deny` references). Wave 1 per-plan-feature-branch protocol per `wave_1_parallel_branch_strategy` (D-43-E6 + project_cross_fork_pr_pattern).
**Cluster-isolation finding (new precedent):** Cherry-pick `5098fc10` introduced a new file `pack_update_hint.rs` that calls `crate::profile::list_pack_store_profiles()` — a function introduced upstream in `24d8b924` (April 2026, pre-v0.53.0) but D-20 manually-replayed in fork's Plan 34-04 WITHOUT the upstream API surface. Fix landed as CR-A `3a5238b2`: added a 73-line fork-side adapter using fork's existing pack-store primitives. Pre-flight detector recommendation: extend to bare `pub fn` references in NEW files, not just `pub use`/`pub mod` re-exports.
**Empty cherry-pick (98c18f1f):** docs-only + edition-2024 let-chains refactor → net Rust diff = 0 bytes after let-chains→nested-if-let conversion. Landed as `git commit --allow-empty` with D-19 trailer for traceability.
**CI baseline diff:** zero `success → failure` transitions vs baseline `13cc0628` (independent worktree-head comparison per `wave_1_parallel_branch_strategy.baseline_ci_gate`). One pre-existing parallel-test env-var-leakage flake (`supervisor::aipc_sdk::tests::windows_loopback_tests::helper_stamps_session_token_from_env`) deferred to D-43-DEF-01 — not caused by Plan 43-03 (test passes at baseline AND in isolation).
**PR body update deferred to orchestrator** per `wave_1_parallel_branch_strategy.umbrella_pr_body_update: orchestrator-post-both-wave-1-plans-close` — orchestrator merges both Wave 1 worktrees before updating umbrella PR body with both contribution sections.
