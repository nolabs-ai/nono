# Phase 41: CI cleanup + v24 broker code-review closure - Pattern Map

**Mapped:** 2026-05-15
**Files analyzed:** 19 (17 modified + 2 created)
**Analogs found:** 19 / 19

## File Classification

| File (Modified/Created) | Role | Data Flow | Closest Analog | Match Quality |
|-------------------------|------|-----------|----------------|---------------|
| `crates/nono-cli/src/exec_strategy.rs` (M) | rust-source (supervisor IPC handler) | request-response | `crates/nono/src/supervisor/aipc_sdk.rs` (SDK builder side of same enum) + self (lines 2682-2810 own analog) | exact (intra-file pattern) |
| `crates/nono-cli/src/audit_ledger.rs` (M — delete) | rust-source (graveyard module) | n/a (delete-class) | `crates/nono-cli/src/main.rs:10` `mod` declaration (the only thing referencing the module) | exact (delete precedent: prior module removals in CLEAN-03) |
| `crates/nono-cli/src/audit_integrity.rs` (M — cfg-gate) | rust-source (audit recorder) | event-driven (CRUD-like append) | `crates/nono-cli/src/exec_strategy.rs:380` `allow_launch_services_active` cfg-gated field | role-match (cross-platform precedent) |
| `crates/nono-cli/src/profile_runtime.rs` (M — `#[allow]` + rationale) | rust-source (test helper inside `cfg(target_os = "linux")` mod) | n/a (Drop primitive) | `crates/nono-cli/src/test_env.rs:24, 56` `EnvVarGuard` impl + Drop blocks | exact (D-08 named this exact analog) |
| `crates/nono-cli/src/exec_strategy.rs:1930` (M — delete unreachable) | rust-source | n/a (delete dead expression) | `crates/nono-cli/src/exec_strategy.rs:1934-1947` `wait_for_child` (the function being shadow-called) | exact (self-context) |
| `scripts/validate-windows-msi-contract.ps1` (M — thread `-BrokerPath`) | powershell-script (validator) | request-response (build invocation) | `scripts/build-windows-msi.ps1:12-13` `[Parameter(Mandatory = $true)] [string]$BrokerPath` declaration + existing `$ServiceBinary` thread-through in `Get-WixDocumentForScope` | exact (`$ServiceBinaryPath` is the precedent) |
| `crates/nono-cli/tests/env_vars.rs:811, 959` (M — investigate, then product fix) | rust-test (integration) | request-response (subprocess invocation) | `crates/nono-cli/src/cli.rs:1637-1640` `dangerous_force_wfp_ready` flag (the product-side fix surface) | role-match (test + flag analog pair) |
| `crates/nono-cli/src/cli.rs:1637-1640` (M — promote flag out of `cfg(debug_assertions)`) | rust-source (clap arg declaration) | n/a (CLI grammar) | `crates/nono-cli/src/cli.rs:1634` `allow_gpu` (sibling `#[arg(long, help_heading = "OPTIONS")]` without debug_assertions gate) | exact (drop one attribute, keep `hide = true`) |
| `crates/nono-cli/tests/env_vars.rs:1028` (M — parallel flake fix) | rust-test (integration) | n/a (env-state isolation) | `crates/nono-cli/src/test_env.rs:28-39` `EnvVarGuard::set_all` (the canonical abstraction per RESEARCH Task 2) | exact (D-08 names this primitive as the standard) |
| `bindings/c/src/lib.rs:132-138` (M — remap + doc rewrite) | rust-source (FFI shim) | request-response (error mapping) | `bindings/c/src/lib.rs:131` `LabelApplyFailed { .. } => NonoErrorCode::ErrSandboxInit` (precedent: same target variant) | exact (RESEARCH cites this as the precedent) |
| `bindings/c/include/nono.h` (M — auto-regenerated) | autogen-header | n/a (build artifact) | Existing `nono.h` (regenerated by cbindgen on every build per `bindings/c/build.rs`) | exact (build-artifact path) |
| `crates/nono-shell-broker/src/main.rs:~93-98` (M — null-handle + empty-list rejects) | rust-source (argv parser) | request-response (parse → Result) | `crates/nono-shell-broker/src/main.rs:88-97` (existing `--inherit-handle` parse + `NonoError::SandboxInit` error shape) | exact (intra-fn extension) |
| `crates/nono-shell-broker/src/main.rs:489-502` (M — flip test) | rust-test (unit) | n/a (assertion pattern flip) | `crates/nono-shell-broker/src/main.rs:507-514` `parse_args_dangling_flag_value_returns_error` (existing "expect SandboxInit error" pattern in same mod) | exact (sibling test in same file) |
| `crates/nono-cli/src/exec_strategy_windows/launch.rs:2450-2460` (M — SKIP → FAIL) | rust-test (integration, Windows-only) | n/a (test-fixture preconditions) | `crates/nono-cli/src/exec_strategy_windows/launch.rs:2472` `assert!(!job.is_null(), ...)` (the test's own fail-loud precedent) | role-match (intra-test escalation) |
| `crates/nono-cli/build.rs:70-84` (M — add broker pre-build block) | build-script | request-response (cargo subprocess OR `[dev-dependencies]`) | `crates/nono-cli/build.rs:70-84` existing Windows-only block (the staging-WFP-driver block is the structural twin) | exact (extension of same `if target_os == windows` shape) |
| `.planning/templates/upstream-sync-quick.md` (M — additive `## Baseline-aware CI gate` section) | config-template (process doc) | n/a (template structure) | `.planning/templates/upstream-sync-quick.md:96` (`## Conflict-file inventory`) + `:138` (`## Fork-divergence catalog`) — both are existing top-level `##` sections in the same file | exact (additive section pattern in same template) |
| `.planning/STATE.md:218-233` (M — clear v24 CR-A row) | project-doc | n/a (table-row removal) | `.planning/STATE.md:222-231` (the same table — row removal is structural, not novel) | exact (intra-table edit) |
| `.planning/phases/41-.../41-SUMMARY.md` (M — frontmatter convention block) | project-doc | n/a (frontmatter convention) | `.planning/STATE.md:188-189` (existing `## Deferred Items` framing) — RESEARCH says "copies the wording from STATE.md's D-40-C2 entries"; structural precedent for `_load_bearing` vs `_environmental` in `.planning/STATE.md:220` ("Gaps are environmental ... + paperwork") | role-match (precedent terminology) |
| `crates/nono-ffi/tests/error_mapping.rs` OR extension to `bindings/c/src/lib.rs` `mod tests` (C — new FFI test) | rust-test (FFI unit) | request-response (Error → Code) | `bindings/c/src/lib.rs:260-272` `action_required_maps_to_err_config_parse` (the existing FFI mapping test that this NEW test directly mirrors) | exact (identical test shape) |
| `crates/nono-shell-broker/src/main.rs` `mod tests` (C — new null-handle reject test) | rust-test (unit, inside source file) | request-response (parse → Result) | `crates/nono-shell-broker/src/main.rs:507-514` `parse_args_dangling_flag_value_returns_error` (sibling test, exact same shape) | exact (sibling test pattern) |

## Pattern Assignments

### `crates/nono-cli/src/exec_strategy.rs` — Plan 41-01 (API migration) + Plan 41-02 (unreachable expression)

**Analog A (Plan 41-01, intra-file at 14 sites):** Self-pattern at lines 2682-2810 — every site reads `request.path` in one of three shapes.

**Imports pattern** (top of file — verify location during spike):
```rust
use nono::CapabilityRequest;
// Helper will need: use nono::HandleTarget;
```

**Helper to ADD** (private to module, placement: above `handle_capability_message` or top of the same scope):
```rust
// Reads the request's path using the AIPC-01 shape (target = Some(FilePath{path}))
// with fallback to the Phase 11 deprecated `path` field. Localizes the
// deprecation surface to ONE place: once Phase 11 wire-shape is retired,
// only this helper changes.
fn request_path(request: &nono::CapabilityRequest) -> &std::path::Path {
    use nono::HandleTarget;
    match &request.target {
        Some(HandleTarget::FilePath { path }) => path.as_path(),
        _ => {
            #[allow(deprecated)]
            { &request.path }
        }
    }
}
```

**14 site-shape mappings** (verified at `exec_strategy.rs:2662-2810`):
```rust
// Shape 1 — owned PathBuf into DenialRecord (7 sites: 2662, 2696, 2729, 2742, 2763, 2781, 2794):
DenialRecord { path: request.path.clone(), ... }
// MIGRATE → DenialRecord { path: request_path(&request).to_path_buf(), ... }

// Shape 2 — display in format!/debug!/warn! (5 sites: 2684, 2690, 2710, 2717, 2757):
debug!("... {} ...", request.path.display());
// MIGRATE → debug!("... {} ...", request_path(&request).display());

// Shape 3 — &Path/&PathBuf parameter pass (2+ sites: 2684, 2710, plus 2808):
overlapping_protected_root(&request.path, ...);
ti.check_path(&request.path)
open_path_for_access(&request.path, &request.access, ...);
// MIGRATE → ...(request_path(&request), ...)
```

**Analog B (Plan 41-02, unreachable expression at line 1930):** Self-context — lines 1934-1947 show the same function being called as a no-op.

**Unreachable expression** (lines 1920-1931 — verified):
```rust
                std::thread::sleep(Duration::from_millis(200));
            }
            Ok(status) => return Ok(status),
            Err(nix::errno::Errno::EINTR) => continue,
            Err(e) => {
                return Err(NonoError::SandboxInit(format!("waitpid() failed: {}", e)));
            }
        }
    }

    wait_for_child(child)  // <-- LINE 1930: UNREACHABLE; delete this line
}
```

The loop above (lines 1913-1928) exits ONLY via `return`. Verify by reading the full surrounding fn during the plan; deletion of line 1930 (and any preceding blank line) is the fix.

---

### `crates/nono-cli/src/audit_ledger.rs` — Plan 41-02 commit 1 (DELETE class)

**Analog:** No direct in-tree analog for "delete entire orphan module"; the closest pattern is the discipline of `git grep` verification before delete.

**Verification pattern** (RESEARCH commands, mandatory before commit):
```bash
# Verify zero non-test consumers across workspace:
git grep -n "audit_ledger::" crates/
git grep -n "mod audit_ledger" crates/
# Expected: only `crates/nono-cli/src/main.rs:10` shows `mod audit_ledger;`.
```

**Post-delete cleanup pattern:** Remove the `mod audit_ledger;` line from `crates/nono-cli/src/main.rs:10` (the single declaration site).

**Commit body shape** (per CONTEXT D-07 — table-in-body):
```
chore(41-02): delete truly-unused orphans

| Symbol | File:line | Grep evidence |
|--------|-----------|---------------|
| AUDIT_LEDGER_FILENAME | audit_ledger.rs:15 | 0 hits outside module |
| ... (17 more rows from RESEARCH inventory) ...

Verified via:
  cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used
```

---

### `crates/nono-cli/src/audit_integrity.rs` — Plan 41-02 commit 2 (CFG-GATE class)

**Analog:** `crates/nono-cli/src/exec_strategy.rs:380` (verified):

```rust
/// Whether direct LaunchServices opening is enabled for this session.
#[cfg_attr(not(target_os = "macos"), allow(dead_code))]
pub allow_launch_services_active: bool,
```

**Pattern to apply** (CONTEXT D-05 disposition tree — Windows-only caller exists at `exec_strategy_windows/supervisor.rs:1832`):
```rust
// At audit_integrity.rs:217 — fn record_capability_decision:
#[cfg(target_os = "windows")]  // OPTION A: scope to Windows entirely
pub(crate) fn record_capability_decision(
    &mut self,
    entry: AuditEntry,
    reject_stage: Option<RejectStage>,
) -> Result<()> { ... }

// OR (matches existing precedent at exec_strategy.rs:380):
#[cfg_attr(not(target_os = "windows"), allow(dead_code))]
pub(crate) fn record_capability_decision(...) { ... }  // OPTION B
```

**Site of the caller** (verified at `crates/nono-cli/src/exec_strategy_windows/supervisor.rs:1828-1832`):
```rust
if let Some(recorder_mutex) = audit_recorder {
    match recorder_mutex.lock() {
        Ok(mut recorder) => {
            if let Err(e) =
                recorder.record_capability_decision(entry.clone(), reject_stage)
```

This single Windows-only caller is what dictates the disposition (per D-05: wire-up not delete).

---

### `crates/nono-cli/src/profile_runtime.rs` — Plan 41-02 disallowed_methods fence (D-08)

**Analog:** `crates/nono-cli/src/test_env.rs:24, 56` (verified, EXACT match cited by CONTEXT D-08):

```rust
#[allow(clippy::disallowed_methods)] // This IS the safe wrapper around env var mutation.
impl EnvVarGuard {
    ...
    pub fn set_all(vars: &[(&'static str, &str)]) -> Self {
        ...
        for (key, value) in vars {
            std::env::set_var(key, value);  // <-- the lint hit; allow at block scope
        }
        ...
    }
}

#[allow(clippy::disallowed_methods)] // Restoring env vars is the other half of the safe wrapper.
impl Drop for EnvVarGuard {
    fn drop(&mut self) {
        for (key, value) in self.original.iter().rev() {
            match value {
                Some(value) => std::env::set_var(key, value),
                None => std::env::remove_var(key),
            }
        }
    }
}
```

**Pattern to apply** (verified target at `profile_runtime.rs:319-347`):
```rust
#[cfg(target_os = "linux")]
struct EnvGuard {
    key: String,
    prior: Option<std::ffi::OsString>,
}

#[allow(clippy::disallowed_methods)] // EnvGuard IS the Drop-restore primitive — same role as
                                     // crate::test_env::EnvVarGuard. Lint applies to consumers.
#[cfg(target_os = "linux")]
impl EnvGuard {
    fn set(key: &str, value: &std::path::Path) -> Self {
        let prior = std::env::var_os(key);
        std::env::set_var(key, value);  // line 331
        Self { key: key.to_string(), prior }
    }
}

#[allow(clippy::disallowed_methods)] // Restoring env vars is the other half of the safe wrapper.
#[cfg(target_os = "linux")]
impl Drop for EnvGuard {
    fn drop(&mut self) {
        match self.prior.take() {
            Some(val) => std::env::set_var(&self.key, val),  // line 343
            None => std::env::remove_var(&self.key),         // line 344
        }
    }
}
```

The 2-line rationale comment is the load-bearing requirement per CONTEXT D-08. RESEARCH also offers an alternative: replace `EnvGuard` with `use crate::test_env::EnvVarGuard` since they're functionally identical — planner discretion per CONTEXT.

---

### `scripts/validate-windows-msi-contract.ps1` — Plan 41-03 (`-BrokerPath` threading)

**Analog A — declaration pattern:** `scripts/build-windows-msi.ps1:1-13` (verified):
```powershell
param(
    [Parameter(Mandatory = $true)]
    [string]$VersionTag,

    [Parameter(Mandatory = $true)]
    [string]$BinaryPath,

    # Phase 31 Plan 04: nono-shell-broker.exe is required for v2.3 SHELL-01 enforcement.
    # The broker installs as a sibling of nono.exe in INSTALLFOLDER so the runtime
    # cascade arm in spawn_windows_child resolves it via current_exe().parent() (D-07).
    # Mandatory so release.yml's invocation cannot accidentally omit it (fail-closed).
    [Parameter(Mandatory = $true)]
    [string]$BrokerPath,
    ...
)
```

**Analog B — thread-through pattern in validator:** `scripts/validate-windows-msi-contract.ps1:11-41` (verified, `$ServiceBinaryPath` is the precedent):
```powershell
function Get-WixDocumentForScope {
    param(
        [Parameter(Mandatory = $true)]
        [string]$Scope,

        [Parameter(Mandatory = $true)]
        [string]$Binary,

        [string]$ServiceBinary = ""   # <-- precedent: optional thread-through
    )
    ...
    $buildArgs = @{
        VersionTag  = "v0.0.0-preview"
        BinaryPath  = $Binary
        Scope       = $Scope
        OutputDir   = $tempDirName
        EmitOnly    = $true
    }
    if ($ServiceBinary -ne "") {
        $buildArgs["ServiceBinaryPath"] = $ServiceBinary    # <-- conditional add (precedent)
    }
    & (Join-Path $PSScriptRoot "build-windows-msi.ps1") @buildArgs
    ...
}
```

**Pattern to apply** (RESEARCH Pattern (1) preferred):
1. Add `[Parameter(Mandatory = $true)] [string]$BrokerPath` to top-level `param(...)` (lines 1-6).
2. Add `[Parameter(Mandatory = $true)] [string]$BrokerBinary` to `Get-WixDocumentForScope`'s `param(...)`.
3. Add `$buildArgs["BrokerPath"] = $BrokerBinary` to the hashtable (mirroring the `$ServiceBinary` conditional shape but unconditional since broker is mandatory).
4. Update both call sites at lines 115-116 to pass `-BrokerBinary $brokerFullPath`.
5. Add `Resolve-Path` for `$BrokerPath` mirroring lines 105-112's `$binaryFullPath` pattern.

---

### `crates/nono-cli/tests/env_vars.rs:773, 916` — Plan 41-04 (block-net probe triage)

**Analog (root-cause + product-side fix surface):** `crates/nono-cli/src/cli.rs:1632-1640` (verified):

```rust
/// framework paths (macOS). On Windows, accepted but not enforced — emits
/// a warning (upstream v0.31–0.33 D-12 + v0.34 D-13).
#[arg(long, help_heading = "OPTIONS")]
pub allow_gpu: bool,

/// Internal: force WFP readiness for test-built Windows binaries.
#[cfg(debug_assertions)]                          // <-- the gate causing release-mode test failure
#[arg(long, hide = true, help_heading = "OPTIONS")]
pub dangerous_force_wfp_ready: bool,
```

**Pattern to apply** (RESEARCH Plan 41-04 hypothesis 1 fix path 1a — promote flag):
```rust
/// Internal: force WFP readiness for test-built Windows binaries.
/// Hidden from --help; intended only for nono-cli test harness.
#[arg(long, hide = true, help_heading = "OPTIONS")]
pub dangerous_force_wfp_ready: bool,
```

Drop the `#[cfg(debug_assertions)]` line. Leave `hide = true` so the flag stays absent from user-facing `--help`. RESEARCH Open Question #3 says Plan 41-04 task 1 should first read `.github/workflows/ci.yml` to confirm CI builds with `--release` before applying this fix.

**Test body analog** (verified at `env_vars.rs:789-804`) — the test invocations themselves do NOT need to change if the flag is promoted out of `cfg(debug_assertions)`:
```rust
let output = nono_bin()
    .args([
        "run",
        "--allow", &allowed,
        "--dangerous-force-wfp-ready",   // <-- this arg currently rejected in release mode
        "--block-net",
        "--workdir", &workdir,
        "--", &probe.to_string_lossy(),
        "--connect-port", &port.to_string(),
    ])
    .output()
    .expect("failed to run nono");
```

If the planner instead picks Pattern (1b) per RESEARCH (change the test build profile), the change goes in `.github/workflows/ci.yml`, not in env_vars.rs.

---

### `crates/nono-cli/tests/env_vars.rs:1028` — Plan 41-05 (parallel flake fix)

**Analog:** `crates/nono-cli/src/test_env.rs:28-39` (verified — the canonical abstraction RESEARCH names):

```rust
#[must_use]
pub fn set_all(vars: &[(&'static str, &str)]) -> Self {
    let original = vars
        .iter()
        .map(|(key, _)| (*key, std::env::var(key).ok()))
        .collect::<Vec<_>>();

    for (key, value) in vars {
        std::env::set_var(key, value);
    }

    Self { original }
}
```

**Pattern to apply** (RESEARCH Plan 41-05 Task 2):
```rust
#[cfg(target_os = "windows")]
#[test]
fn windows_run_redirects_profile_state_vars_into_writable_allowlist() {
    // Pin a canonical Windows runtime baseline before invoking nono_bin().
    // This isolates the test from any parallel test that mutates these vars.
    let _env = crate::test_env::EnvVarGuard::set_all(&[
        ("PATH",       r"C:\Windows\system32;C:\Windows"),
        ("PATHEXT",    ".COM;.EXE;.BAT;.CMD"),
        ("COMSPEC",    r"C:\Windows\system32\cmd.exe"),
        ("SystemRoot", r"C:\Windows"),
        ("windir",     r"C:\Windows"),
        ("SystemDrive", "C:"),
    ]);
    // ... rest of existing test body unchanged ...
}
```

The Drop on `_env` restores originals on test exit. RESEARCH Task 2-alt offers `test_env::lock_env()` as a coarser serialization fallback.

**Note on visibility:** `test_env.rs` is `crates/nono-cli/src/test_env.rs` (a library module). Integration tests in `crates/nono-cli/tests/` may need a `pub use` re-export. Verify during the plan whether `EnvVarGuard` is already exposed; if not, mirror the existing `pub` declarations at lines 10, 12, 20.

---

### `bindings/c/src/lib.rs:132-138` — Plan 41-06 CR-01 (FFI remap)

**Analog:** `bindings/c/src/lib.rs:131` (verified, EXACT precedent from RESEARCH):

```rust
nono::NonoError::LabelApplyFailed { .. } => NonoErrorCode::ErrSandboxInit,
```

This is the existing same-target precedent — both `LabelApplyFailed` and (post-fix) `BrokerNotFound` map to `ErrSandboxInit = -6` because both are "sandbox init failed" semantic class.

**Current state to rewrite** (verified at lines 132-138):
```rust
// Phase 31 D-07: BrokerNotFound is a path-resolution failure (the
// broker.exe sibling lookup against std::env::current_exe() parent
// returned a path that does not exist on disk). Map to
// ErrPathNotFound for FFI consumers — same semantic class as
// PathNotFound, just specifically named for the broker discovery
// call site.
nono::NonoError::BrokerNotFound { .. } => NonoErrorCode::ErrPathNotFound,
```

**Pattern to apply** (CONTEXT D-09 + RESEARCH suggested rewrite):
```rust
// Phase 41 D-09 (CR-01): BrokerNotFound is an installation/runtime
// defect — the broker.exe sibling is missing from disk where
// current_exe().parent() expected it. This is structurally a sandbox-
// init failure (the supervisor cannot stand up its enforcement
// primitive), NOT a user-input path-resolution failure. Map to
// ErrSandboxInit alongside LabelApplyFailed.
nono::NonoError::BrokerNotFound { .. } => NonoErrorCode::ErrSandboxInit,
```

**FFI test analog** (verified at `bindings/c/src/lib.rs:258-272`) — EXACT shape for the new CR-01 test:
```rust
/// Phase 36.5 D-36.5-B3: ActionRequired maps to ErrConfigParse (-9) in the
/// C FFI layer. No new error code value is introduced.
#[test]
fn action_required_maps_to_err_config_parse() {
    let err = nono::NonoError::ActionRequired {
        expected: String::new(),
        actual: String::new(),
        resolve_via: "x".into(),
    };
    let code = map_error(&err);
    assert!(
        matches!(code, types::NonoErrorCode::ErrConfigParse),
        "ActionRequired must map to ErrConfigParse; got {code:?}"
    );
}
```

**Pattern to apply** for the NEW CR-01 test (place in same `mod tests` at `bindings/c/src/lib.rs:255+`):
```rust
/// Phase 41 D-09 (CR-01): BrokerNotFound maps to ErrSandboxInit (-6),
/// NOT ErrPathNotFound (-1). The broker-discovery failure is an
/// installation/runtime defect (sandbox cannot init), not a user-input
/// path-resolution failure. Locks the D-09 mapping against regression.
#[test]
fn broker_not_found_maps_to_err_sandbox_init() {
    let err = nono::NonoError::BrokerNotFound {
        // Construct per the variant's actual field shape — verify during plan.
        path: std::path::PathBuf::from(r"C:\fake\nono-shell-broker.exe"),
    };
    let code = map_error(&err);
    assert!(
        matches!(code, types::NonoErrorCode::ErrSandboxInit),
        "BrokerNotFound must map to ErrSandboxInit; got {code:?}"
    );
}
```

(Planner verifies the actual `BrokerNotFound` field shape — RESEARCH did not enumerate it.)

---

### `bindings/c/include/nono.h` — Plan 41-06 CR-01 (auto-regen)

**Analog:** Existing `nono.h` — auto-generated by cbindgen during the FFI build.

**Pattern:** No hand-edit. After updating `bindings/c/src/lib.rs:132-138` and the doc-comment block above the match arm, regenerate via:
```bash
cargo build -p nono-ffi   # cbindgen runs from bindings/c/build.rs
```

Verify the doc-comment delta with `git diff bindings/c/include/nono.h` (RESEARCH Pitfall 5).

---

### `crates/nono-shell-broker/src/main.rs:~93-98` — Plan 41-06 CR-02 + CR-03 (argv parser rejects)

**Analog:** Same file lines 75-122 (verified — the existing parse pattern):

```rust
"--inherit-handle" => {
    let v = iter.next().ok_or_else(|| {
        NonoError::SandboxInit("--inherit-handle requires a hex value".into())
    })?;
    let hex_str = v.to_string_lossy();
    let stripped = hex_str.trim_start_matches("0x").trim_start_matches("0X");
    let raw_value = usize::from_str_radix(stripped, 16).map_err(|e| {
        NonoError::SandboxInit(format!(
            "--inherit-handle parse error for '{hex_str}': {e}"
        ))
    })?;
    inherit_handles.push(raw_value as HANDLE);     // <-- CR-02 inserts BEFORE this line
}
```

**Pattern to apply for CR-02** (RESEARCH-verified shape, insert after line 97, before line 98):
```rust
if raw_value == 0 || raw_value == usize::MAX {
    return Err(NonoError::SandboxInit(format!(
        "--inherit-handle value '{hex_str}' is null or INVALID_HANDLE_VALUE; reject"
    )));
}
inherit_handles.push(raw_value as HANDLE);
```

**Pattern to apply for CR-03** (verified — insert after `cwd = cwd.ok_or_else(...)?` at line 116, before `Ok(BrokerArgs { ... })` at line 117):
```rust
if inherit_handles.is_empty() {
    return Err(NonoError::SandboxInit(
        "--inherit-handle list is empty; broker requires at least one inheritable handle".into()
    ));
}
Ok(BrokerArgs {
    shell_path,
    shell_args,
    inherit_handles,
    cwd,
})
```

---

### `crates/nono-shell-broker/src/main.rs:489-502` — Plan 41-06 CR-03 (flip existing test)

**Analog A — the existing test (BEFORE):** Verified at lines 489-502:
```rust
/// D-02: an empty inherit-handle list is the most-restrictive (and
/// expected) shape — it means the spawned child inherits NO handles.
/// Construction MUST succeed with `inherit_handles.is_empty()`.
#[test]
fn parse_args_empty_inherit_handle_list_is_ok() {
    let raw = argv(&["--shell", "foo", "--cwd", r"C:\"]);
    let parsed = parse_args(&raw).expect("parse must succeed with no --inherit-handle");
    assert!(
        parsed.inherit_handles.is_empty(),
        "no --inherit-handle flags → empty list (most-restrictive shape)"
    );
    ...
}
```

**Analog B — the sibling "expected error" test pattern:** Verified at lines 507-514:
```rust
/// Defensive parse: a flag at the end of argv with no following value
/// MUST fail — silently treating it as an empty string would let a
/// truncated argv slip through (e.g., from a corrupted IPC channel).
#[test]
fn parse_args_dangling_flag_value_returns_error() {
    let raw = argv(&["--cwd", r"C:\", "--shell"]);
    let Err(NonoError::SandboxInit(msg)) = parse_args(&raw) else {
        panic!("expected SandboxInit error when --shell has no value");
    };
    assert!(
        ...
    );
}
```

**Pattern to apply (CR-03 FLIPPED):**
```rust
/// Phase 41 D-12 (CR-03): an empty inherit-handle list is REJECTED at the
/// broker argv parser. Supersedes Plan 31-02 SUMMARY's "most-restrictive"
/// claim — the broker now requires at least one inheritable handle, making
/// the empty-list shape correct-by-construction-rejected.
#[test]
fn parse_args_empty_inherit_handle_list_returns_error() {
    let raw = argv(&["--shell", "foo", "--cwd", r"C:\"]);
    let Err(NonoError::SandboxInit(msg)) = parse_args(&raw) else {
        panic!("expected SandboxInit error on empty --inherit-handle list");
    };
    assert!(
        msg.contains("empty"),
        "error message must indicate empty-list rejection, got: {msg}"
    );
}
```

**Pattern to apply for the NEW CR-02 null-handle test** (place adjacent in same mod, mirrors Analog B):
```rust
/// Phase 41 D-11 (CR-02): a null or INVALID_HANDLE_VALUE handle is REJECTED
/// at the broker argv parser. Pseudo-handle confusion at `(HANDLE)0` and
/// the `(HANDLE)-1` sentinel are blocked before any UpdateProcThreadAttribute
/// call. Locks the CR-02 fix against regression.
#[test]
fn parse_args_null_inherit_handle_returns_error() {
    let raw = argv(&["--shell", "foo", "--cwd", r"C:\", "--inherit-handle", "0x0"]);
    let Err(NonoError::SandboxInit(msg)) = parse_args(&raw) else {
        panic!("expected SandboxInit error on --inherit-handle 0x0");
    };
    assert!(
        msg.contains("null") || msg.contains("INVALID_HANDLE_VALUE"),
        "error message must indicate null-handle rejection, got: {msg}"
    );
}
```

---

### `crates/nono-cli/src/exec_strategy_windows/launch.rs:2450-2460` — Plan 41-07 CR-04 (SKIP → FAIL)

**Analog:** Same-file precedent at line 2472 (verified — the test's own fail-loud pattern):
```rust
assert!(!job.is_null(), "CreateJobObjectW returned null handle");
```

**Current state to replace** (verified at lines 2445-2460):
```rust
let broker_path = if candidate_triple.exists() {
    candidate_triple
} else if candidate_default.exists() {
    candidate_default
} else {
    eprintln!(
        "SKIP: broker artifact missing at {} and {} — pre-build via \
         `cargo build -p nono-shell-broker --release --target x86_64-pc-windows-msvc` \
         to exercise D-04 Job Object containment locally. The Plan 31-05 \
         field-test runner has the artifact via Plan 31-04's release \
         pipeline guarantee.",
        candidate_triple.display(),
        candidate_default.display()
    );
    return;
};
```

**Pattern to apply** (RESEARCH-verified shape, CONTEXT D-13):
```rust
let broker_path = if candidate_triple.exists() {
    candidate_triple
} else if candidate_default.exists() {
    candidate_default
} else {
    panic!(
        "nono-shell-broker.exe missing at {} and {}; pre-build with \
         `cargo build -p nono-shell-broker --release` (or set the broker pre-build \
         via crates/nono-cli/build.rs per Phase 41 D-14). This test asserts \
         Job Object containment is enforced before ResumeThread and cannot be \
         silently skipped — see Phase 41 CR-04 disposition.",
        candidate_triple.display(),
        candidate_default.display()
    );
};
```

The `panic!` has type `!` which coerces to the `PathBuf` the surrounding `if/else if/else` expression expects. No `return;` needed.

---

### `crates/nono-cli/build.rs` — Plan 41-07 D-14 (broker pre-build extension point)

**Analog:** Same-file existing Windows-only block at lines 69-84 (verified — RESEARCH Pattern):

```rust
// === Stage placeholder Windows WFP driver artifact next to build outputs ===
if env::var("CARGO_CFG_TARGET_OS").as_deref() == Ok("windows") {
    let driver_path = Path::new("data/windows/nono-wfp-driver.sys");
    println!("cargo:rerun-if-changed={}", driver_path.display());

    if driver_path.exists() {
        if let Some(profile_dir) = out_path.ancestors().nth(3) {
            fs::copy(driver_path, profile_dir.join("nono-wfp-driver.sys"))
                .expect("Failed to copy placeholder WFP driver artifact to build output");
        } else {
            println!("cargo:warning=Could not determine target profile dir for placeholder WFP driver artifact");
        }
    } else {
        println!("cargo:warning=data/windows/nono-wfp-driver.sys not found");
    }
}
```

**Pattern to apply — Option A (cargo subprocess, RESEARCH-warned re: recursion):**
```rust
// === Pre-build nono-shell-broker on Windows (Phase 41 D-14, CR-04) ===
// Job-object integration test at exec_strategy_windows/launch.rs:2423 now
// panics on missing broker artifact (D-13). build.rs makes the artifact
// automatic to eliminate developer-memory-burden per CONTEXT D-14.
if env::var("CARGO_CFG_TARGET_OS").as_deref() == Ok("windows") {
    // Use a separate --target-dir to sidestep cargo's process-global lock
    // (RESEARCH Pitfall 6).
    let nested_target = out_path.join("broker-prebuild");
    let status = std::process::Command::new(env::var("CARGO").unwrap_or_else(|_| "cargo".into()))
        .args(["build", "-p", "nono-shell-broker", "--release"])
        .args(["--target-dir", &nested_target.to_string_lossy()])
        .status();
    match status {
        Ok(s) if s.success() => {}
        Ok(s) => println!("cargo:warning=nono-shell-broker pre-build exited with {s}"),
        Err(e) => println!("cargo:warning=nono-shell-broker pre-build failed to spawn: {e}"),
    }
}
```

**Pattern to apply — Option B (`[dev-dependencies]`, RESEARCH-preferred):** No `build.rs` change. Instead, edit `crates/nono-cli/Cargo.toml`:
```toml
[target.'cfg(target_os = "windows")'.dev-dependencies]
nono-shell-broker = { path = "../nono-shell-broker", version = "0.53.0" }
```

Cargo will then build the broker before running `nono-cli` tests. RESEARCH says verify there's no workspace cycle (broker must not depend on `nono-cli`).

---

### `.planning/templates/upstream-sync-quick.md` — Plan 41-07 D-16 (additive baseline section)

**Analog:** Same-file existing top-level `##` sections at lines 46 (`## Drift inventory`), 96 (`## Conflict-file inventory`), 117 (`## Windows-specific retrofit checklist`), 138 (`## Fork-divergence catalog`) — verified structural precedent for additive sections.

**Pattern to apply** (RESEARCH-confirmed: this is ADDITIVE, not find-replace — Pitfall 4):

Insert AFTER the `## Drift inventory` section (line 46-94) and BEFORE `## Conflict-file inventory` (line 96):

```markdown
---

## Baseline-aware CI gate

For upstream-sync waves landing on top of a known-green baseline, the close
gate flags ONLY `success → failure` transitions vs the baseline. Drift
accumulates across milestones; reset at each milestone-internal cleanup.

**Current baseline SHA:** `<Phase 41 close SHA — set after Phase 41 ships>`
**Last reset:** Phase 41 close (REQ-CI-03), 2026-05-15 → cleaning the
pre-existing red carried forward from baseline `a72736bb`.
**Reset cadence:** Every milestone-internal cleanup phase (see Phase 41 for
the v2.5 precedent).

CI gate result interpretation:
- Lane was green on baseline AND is green on PR head: PASS.
- Lane was green on baseline AND is red on PR head: FAIL (real regression).
- Lane was red on baseline AND is red on PR head: PASS (carry-forward, not
  introduced by this PR).
- Lane was red on baseline AND is green on PR head: PASS + IMPROVEMENT.

---
```

The literal SHA placeholder is intentional — RESEARCH notes the actual SHA is unknown until Phase 41 merges (Plan 41-07 final task fills it from PR-head SHA, or commits with placeholder and follows up post-merge).

---

### `.planning/STATE.md` — Plan 41-07 D-16 commit 3 (clear v24 CR-A row)

**Analog:** Intra-table edit at lines 218-233 (verified — the row to remove and the summary line to update).

**Current state to edit** (verified at lines 222-228):
```markdown
| Category | Slug / File | Status | Disposition |
|----------|-------------|--------|-------------|
| requirement | REQ-RESL-NIX-01..03 | re-anchored to v2.5 (Phase 37) | ... |
| requirement | REQ-PKGS-04 | re-anchored to v2.5 (Phase 37) | ... |
| requirement | REQ-AAHX-HOST-01 | re-anchored to v2.5 (Phase 38) | ... |
| quick_task × 20 | 260405..260514 entries | "missing" per audit-open | ... |
| todo × 4 | v24-cr-0[1-4]-* | pending | v24 code-review todos ... |   <-- REMOVE this row
| uat_gap × 11 | various | ... |
| verification_gap × 7 | various | ... |
| context_question × 1 | 25-CONTEXT.md | n/a | ... |
```

**Current summary line to update** (verified at line 233):
```markdown
Known deferred items at v2.4 close: 5 host-blocked requirements (re-anchored to v2.5) + 4 v24 CR todos + ~18 human-verify items (Phase 35/36) + 1 context question + 20 audit-open cataloging glitches. None block release; cross-phase integration confirmed clean.
```

**Pattern to apply:** Remove the `| todo × 4 |` row entirely. Update the summary line to drop `+ 4 v24 CR todos` and decrement total accordingly (e.g., from implied total to total minus 4). Add a Phase 41 reference clause: `Resolved by Phase 41 (PR #<N>); cleared 2026-05-XX.`

---

### `.planning/phases/41-.../41-SUMMARY.md` — Plan 41-07 D-16 commit 2 (frontmatter convention block)

**Analog:** No exact in-tree analog for a "frontmatter convention block in a SUMMARY"; closest pattern is `.planning/STATE.md:220` (verified) which uses the `_environmental` / `_load_bearing` language informally:

```markdown
Gaps are environmental (Linux/macOS host availability) + paperwork
(audit-open cataloging glitch on nested quick-task directories).
```

**Pattern to apply** (RESEARCH-derived convention block — written by `/gsd-complete-phase`; PATTERNS.md provides the wording the planner inserts):
```markdown
---
skipped_gates_convention:
  load_bearing: |
    Gates that MUST pass and were skipped due to a load-bearing reason that
    CI compensates for. Example: cross-target clippy gates 3+4 skipped because
    the Windows dev host lacks C cross-compilers (aws-lc-sys, ring). The GAP
    is real and the CI Linux + macOS native lanes cover it.
  environmental: |
    Gates that don't apply to this run. Example: a macOS-only test skipped on
    a Linux runner. Not load-bearing — the gate would never have provided
    signal on this run.
---
```

Planner places this at the top of `41-SUMMARY.md` immediately after the existing frontmatter block (or as its replacement if SUMMARY frontmatter is empty).

---

## Shared Patterns

### Cross-target clippy verification (CONTEXT D-06; memory `feedback_clippy_cross_target`)

**Source:** RESEARCH § "Verification Standard" + Phase 25 CR-A regression lesson

**Apply to:** Every commit in Plans 41-01 and 41-02, AND every Linux-touching commit in Plans 41-04 + 41-05 + 41-06.

```bash
# Mandatory before each Linux-touching commit (CONTEXT D-06):
cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used

# Recommended (CI compensates if local toolchain unavailable):
cargo clippy --workspace --target x86_64-apple-darwin -- -D warnings -D clippy::unwrap_used
```

### `NonoError::SandboxInit` for broker argv failures

**Source:** `crates/nono-shell-broker/src/main.rs:78, 84, 89, 95` (verified)

**Apply to:** All CR-02 + CR-03 new error returns in Plan 41-06.

```rust
return Err(NonoError::SandboxInit(format!("<structured message>")));
```

All broker-parse failures share this error class — keep it consistent.

### Test pattern: `let Err(SandboxInit(msg)) = parse_args(&raw) else { panic!(...); };`

**Source:** `crates/nono-shell-broker/src/main.rs:510-513` (verified `parse_args_dangling_flag_value_returns_error`)

**Apply to:** All new + flipped tests in Plan 41-06 CR-02 + CR-03.

```rust
let Err(NonoError::SandboxInit(msg)) = parse_args(&raw) else {
    panic!("expected SandboxInit error on <input>");
};
assert!(msg.contains("<keyword>"), "got: {msg}");
```

### DCO sign-off on every commit (CLAUDE.md § Coding Standards)

**Source:** CLAUDE.md

**Apply to:** Every commit in Plans 41-01 through 41-07.

```
Signed-off-by: <name> <email>
```

### Per-file `#[allow(clippy::unwrap_used)]` on test modules (CLAUDE.md § Unwrap Policy)

**Source:** `bindings/c/src/lib.rs:253-254` (verified):
```rust
#[cfg(test)]
#[allow(clippy::unwrap_used)]
mod tests {
```

**Apply to:** Any new `mod tests` block added by Plan 41-06 (FFI mapping test) or Plan 41-07 (if new test scaffolding is needed).

### Commit body table convention (CONTEXT D-07)

**Source:** CONTEXT D-07 + memory `project_workspace_crates` (CR-A atomic-commit hygiene)

**Apply to:** Plan 41-02's three commits.

```
chore(41-02): <disposition class>

| Symbol | File:line | Action | Evidence |
|--------|-----------|--------|----------|
| ... | ... | delete | grep: 0 hits |
| ... | ... | wire-up via cfg | caller at <path>:<line> |

Verified via:
  cargo clippy --workspace --target x86_64-unknown-linux-gnu -- -D warnings -D clippy::unwrap_used
```

## No Analog Found

All 19 files have at least a role-match or exact analog in the codebase. No "novel pattern" files in this phase — Phase 41 is mechanical cleanup, and every site has either an intra-file or sibling-file precedent.

## Metadata

**Analog search scope:**
- `crates/nono-cli/src/` (full)
- `crates/nono-shell-broker/src/` (full)
- `crates/nono/src/supervisor/types.rs` (HandleTarget shape)
- `bindings/c/src/` (full)
- `crates/nono-cli/tests/env_vars.rs` (test bodies)
- `scripts/*.ps1` (MSI validator + builder)
- `.planning/templates/upstream-sync-quick.md` (template structure)
- `.planning/STATE.md` (deferred-items table)

**Files scanned:** ~25 source files + 2 PowerShell scripts + 2 process docs

**Pattern extraction date:** 2026-05-15

**Key verifications:**
- `audit_integrity::record_capability_decision` caller at `exec_strategy_windows/supervisor.rs:1832` — VERIFIED (cfg-gate disposition correct)
- `bindings/c/src/lib.rs` has existing `mod tests` at line 255 with `action_required_maps_to_err_config_parse` at line 261 — VERIFIED (CR-01 test goes here, not in a new `crates/nono-ffi/tests/error_mapping.rs` file; the workspace crate is `bindings/c/`, not `crates/nono-ffi/`)
- `crates/nono-cli/build.rs:70-84` existing Windows-only block — VERIFIED (extension point for D-14)
- `.planning/templates/upstream-sync-quick.md` — verified NO existing `## Baseline-aware CI gate` section; additive insertion confirmed
- `crates/nono-cli/src/profile_runtime.rs:319-347` `EnvGuard` impl — VERIFIED (the actual disallowed_methods origin per RESEARCH § Site Correction)
- `crates/nono-cli/src/exec_strategy.rs:1920-1931` — VERIFIED (unreachable `wait_for_child(child)` at line 1930; the loop above exits only via `return`)
- `crates/nono-shell-broker/src/main.rs:489-502` `parse_args_empty_inherit_handle_list_is_ok` — VERIFIED (the test to flip per D-12)
- `bindings/c/src/lib.rs:131` `LabelApplyFailed → ErrSandboxInit` — VERIFIED (the precedent CONTEXT D-09 cites)

**Pattern-correction note:** The "new file" `crates/nono-ffi/tests/error_mapping.rs` named in the prompt does NOT match the workspace layout — the FFI crate's root is `bindings/c/src/lib.rs` and its tests live in the same file's `#[cfg(test)] mod tests` block (lines 250-355). The new CR-01 test should be added inline there, mirroring the existing `action_required_maps_to_err_config_parse` test, NOT in a separate `tests/` directory file. RESEARCH § Wave 0 Gaps acknowledges this ambiguity: "The planner picks the location based on where the existing `nono_error_to_error_code` function's test coverage lives." That location is `bindings/c/src/lib.rs:255+`.
