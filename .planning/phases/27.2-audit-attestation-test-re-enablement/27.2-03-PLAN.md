---
phase: 27.2-audit-attestation-test-re-enablement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/audit-bundle-target.md
  - .planning/phases/27.1-nono-test-home-seam/deferred-items.md
autonomous: true
requirements:
  - REQ-AAHX-02
tags: [adr, documentation, audit-bundle-target, deferred-items, v2.5-followups]

must_haves:
  truths:
    - "ADR exists at `docs/architecture/audit-bundle-target.md`."
    - "ADR mirrors the heading structure of `docs/architecture/aipc-unix-futures.md` (Status frontmatter + Context with Goals/Non-goals + Decision Table + Decision + Consequences split Positive/Negative + References split Internal/Source/External)."
    - "ADR contains the option matrix A/B/C with trade-offs."
    - "ADR explicitly supersedes Plan 22-05a Decision 5's 'for backward compatibility' dual-location rationale."
    - "ADR documents the v2.5 shim-removal milestone."
    - "ADR documents the backward-compat shim contract: when the warning fires, what action it instructs, when the shim deletes."
    - "`.planning/phases/27.1-nono-test-home-seam/deferred-items.md` has new H2 section for Phase 27.2 v2.5 follow-ups with H3 entries for v2.5-FU-1 (shim removal) and v2.5-FU-2 (cmd_verify v2 JSON schema)."
  artifacts:
    - path: "docs/architecture/audit-bundle-target.md"
      provides: "Audit bundle target ADR"
      min_lines: 90
      contains: "## Decision Table"
    - path: ".planning/phases/27.1-nono-test-home-seam/deferred-items.md"
      provides: "v2.5 follow-up entries appended"
      contains: "v2.5-FU-1"
  key_links:
    - from: "docs/architecture/audit-bundle-target.md"
      to: "Plan 22-05a Decision 5 supersession"
      via: "Status frontmatter Supersedes field"
      pattern: "Supersedes:.*22-05a"
    - from: "docs/architecture/audit-bundle-target.md"
      to: "v2.5-FU-1 milestone"
      via: "Backward-compat shim contract -> Removal milestone"
      pattern: "v2.5-FU-1"
    - from: ".planning/phases/27.1-nono-test-home-seam/deferred-items.md"
      to: "ADR cross-link"
      via: "v2.5-FU-1 body links to docs/architecture/audit-bundle-target.md"
      pattern: "audit-bundle-target\\.md"
---

<objective>
FU-2 (ADR half): Author the architectural decision record at `docs/architecture/audit-bundle-target.md` recording the Option A choice locked at /gsd-discuss-phase 27.2 (D-27.2-01..06). Mirror the heading structure of `docs/architecture/aipc-unix-futures.md` (Phase 25-02 ADR convention; explicit per D-27.2-03). Append v2.5-FU-1 and v2.5-FU-2 entries to `.planning/phases/27.1-nono-test-home-seam/deferred-items.md` per CONTEXT § Deferred Ideas.

Purpose: Closes REQ-AAHX-02's documentation half. The ADR is the durable record that future readers consult when the v2.5 shim-removal milestone is scheduled, and when external tooling questions why the bundle moved out of `<rollback>/<id>/`. The deferred-items.md append surfaces the two v2.5 follow-ups (shim removal, JSON schema bump) into the milestone-level backlog so v2.5 planning has them on hand.

Output: One new file (`docs/architecture/audit-bundle-target.md`); one modified planning artifact (`.planning/phases/27.1-nono-test-home-seam/deferred-items.md`). No source code changes. `crates/nono/` byte-identical (D-27.2-11). This plan runs in parallel with Plans 27.2-01 and 27.2-02 (Wave 1) — file-disjoint with both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27.2-audit-attestation-test-re-enablement/27.2-CONTEXT.md
@.planning/phases/27.2-audit-attestation-test-re-enablement/27.2-PATTERNS.md
@docs/architecture/aipc-unix-futures.md
@.planning/phases/27.1-nono-test-home-seam/deferred-items.md

<style_invariants>
<!-- Extracted from `docs/architecture/aipc-unix-futures.md` and the existing
     deferred-items.md "Phase 27.1 Plan 03 v2.4 production follow-ups" section.
     Use these directly as templates. -->

ADR style invariants from `docs/architecture/aipc-unix-futures.md`:
- Status frontmatter with these exact 4 keys (lines 1-6 of the analog):
  ```
  # Audit Bundle Target

  **Status:** Accepted
  **Date:** 2026-05-05
  **Phase:** 27.2 (v2.3 Audit-Attestation Test Re-Enablement)
  **Requirement:** REQ-AAHX-02
  **Supersedes:** Plan 22-05a Decision 5 ("for backward compatibility" dual-location rationale)
  ```
  The `Supersedes:` field is added per D-27.2-03 (the analog has only 4 fields; this ADR has 5 because it supersedes a prior decision).
- H2 sections in this exact order:
  1. `## Context` (with H3 subsections: `### Goals` and `### Non-goals`)
  2. `## Decision Table`
  3. `## Decision`
  4. `## Consequences` (with H3 subsections: `### Positive`, `### Negative`, plus topic-specific `### Backward-compat shim contract`)
  5. `## References` (with H3 subsections: `### Internal`, `### Source code`. The analog uses `### External` too — omit if not applicable.)

deferred-items.md style invariants from the existing § "Phase 27.1 Plan 03 v2.4 production follow-ups" section (file lines 52-99):
- H2 section name shape: `## Phase 27.2 v2.5 production follow-ups (audit-verify surface coordinated bump)`
- Two lines below H2 before the first H3:
  ```
  **Created during:** Phase 27.2 close (REQ-AAHX-01..03 closed via D-27.2-01..13)
  **Status:** Surfaced; deferred to v2.5 milestone
  ```
- One H3 per FU: `### v2.5-FU-N: <one-line description>`
- Body fields per H3 (in this order): `**Issue:**`, `**Fix:**`, `**Estimated effort:**`
</style_invariants>

# Project rules (must follow)

- **Markdown only** — no source code in this plan. `crates/nono/` byte-identical (D-27.2-11) is trivially preserved.
- **CLAUDE.md § Commits:** DCO sign-off mandatory.
- **PATTERNS.md cross-reference:** The full ADR template (with all required substantive content — option-matrix verdicts, supersession note, v2.5 milestone, shim contract) is recorded verbatim in 27.2-PATTERNS.md § "docs/architecture/audit-bundle-target.md — NEW ADR (FU-2 ADR)" lines 421-510. Use that template as the literal scaffold; it has been pre-validated against the analog.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the audit-bundle-target.md ADR</name>
  <files>docs/architecture/audit-bundle-target.md</files>
  <read_first>
    - .planning/phases/27.2-audit-attestation-test-re-enablement/27.2-PATTERNS.md (full read of "docs/architecture/audit-bundle-target.md — NEW ADR (FU-2 ADR)" section, lines ~421-510 — the ADR scaffold and style invariants)
    - .planning/phases/27.2-audit-attestation-test-re-enablement/27.2-CONTEXT.md (D-27.2-01, D-27.2-02, D-27.2-03; § Decisions for option-matrix rationale)
    - docs/architecture/aipc-unix-futures.md lines 1-50 (Status frontmatter + Context + Goals + Non-goals — copy heading shapes verbatim)
    - .planning/phases/27.1-nono-test-home-seam/27.1-03-SUMMARY.md § "Gap 2 (Test 2 failure): Bundle target mismatch" (origin of FU-2; ADR cites this as the surfaced bug)
  </read_first>
  <action>
1. Create the file `docs/architecture/audit-bundle-target.md` (use the Write tool — directory `docs/architecture/` already exists, confirmed by `aipc-unix-futures.md` sibling). Use the EXACT content below (planner-authored ADR satisfying all D-27.2-03 substantive requirements + style invariants from `aipc-unix-futures.md`):

```markdown
# Audit Bundle Target

**Status:** Accepted
**Date:** 2026-05-05
**Phase:** 27.2 (v2.3 Audit-Attestation Test Re-Enablement)
**Requirement:** REQ-AAHX-02
**Supersedes:** Plan 22-05a Decision 5 ("for backward compatibility" dual-location rationale)

## Context

Phase 22-05a Decision 5 (v2.2) split audit-only sessions to `<audit_root>/<id>/` "for namespace separation" while keeping rollback-active sessions writing the audit attestation bundle to `<rollback_root>/<id>/audit-attestation.bundle`. The split was a partial migration: audit-only flows landed in the new namespace; `--rollback` flows did not. The disposition was recorded as "for backward compatibility" — meaning, in practice, that v2.2 deferred the question of whether the dual-location shape was the desired endpoint.

Phase 27.1 Plan 03 (v2.3, 2026-05-04) attempted to re-enable Phase 27's deferred audit-attestation integration tests on a Windows host using the Phase 27.1 `NONO_TEST_HOME` seam. The seam itself worked end-to-end (`nono run --audit-integrity` correctly wrote sessions to `<NONO_TEST_HOME>/.nono/audit/<id>/`), but Test 2 (`rollback_signed_session_verifies_from_audit_dir_bundle`) failed because the supervisor wrote the bundle to `<rollback_root>/<id>/` while the test asserted `<audit_root>/<id>/`. Phase 27.1 D-27.1-14 large-fix branch surfaced this as v2.4-FU-2 ("bundle-target architecture decision") and re-`#[ignore]`'d the test pending the design call.

The deeper issue surfaced is non-repudiation: under the v2.2 dual-location shape, `rm -rf ~/.nono/rollbacks/<id>/` deletes the audit attestation bundle along with the snapshot. An adversary (or a careless cleanup script) can destroy the cryptographic evidence that the session ever ran. Audit attestation is the non-repudiation mechanism; locating it inside a directory designed for mutable, user-discardable rollback artifacts is a structural mismatch.

This ADR finishes the namespace-separation migration started in v2.2: the bundle ALWAYS lives at `<audit_root>/<id>/audit-attestation.bundle` regardless of `--rollback` flag state. Snapshot artifacts (`changes/`, `manifest.json`) continue to live under `<rollback_root>/<id>/` when `--rollback` is set. A back-compat verification shim preserves the ability to verify bundles signed by older `nono` versions whose bundles are still at the legacy `<rollback>/<id>/` location.

### Goals

This ADR commits to:

- One canonical bundle location independent of `--rollback` flag state.
- Audit attestation bundle outlives rollback cleanup (`rm -rf ~/.nono/rollbacks/<id>/` no longer destroys it).
- Backward-compatible verification of bundles written by older `nono` versions, with a one-shot deprecation warning so operators can re-sign at their leisure.
- A documented v2.5 milestone for hard-cutover removal of the back-compat shim.

### Non-goals

This ADR explicitly does NOT commit to:

- Migrating existing on-disk bundles. Verification still works via the shim; re-signing is operator-driven.
- Changing `cmd_verify`'s JSON output schema in this phase. The current flat shape (`json["attestation_present"]`, `json["attestation_valid"]`) is preserved verbatim. The richer nested shape is deferred to v2.5-FU-2.
- Touching `crates/nono/` (D-27.2-11 invariant). All code changes live in `crates/nono-cli/`.
- Resolving the WR-01 split-brain home-resolution finding from Phase 27.1 (`validated_home()` vs `nono_home_dir()`). Out of scope per CONTEXT § Deferred Ideas.

## Decision Table

| Option | Bundle Target on `--rollback` | Verification Path | Verdict |
|--------|-------------------------------|-------------------|---------|
| **A (chosen)** | `<audit_root>/<id>/audit-attestation.bundle` always | Audit-first lookup; rollback-root fallback shim with one-shot `tracing::warn!` until v2.5 | **Accepted** |
| B | `<rollback_root>/<id>/...` when `--rollback`; `<audit_root>/<id>/...` otherwise; verify learns dual-root | Permanent dual-root | Rejected: codifies the dual-location complexity instead of finishing the namespace migration. Permanent verifier complexity for no security gain. |
| C | Test rewrite — accept current production behavior (bundle at `<rollback>/<id>/` when rollback-active) | Unchanged | Rejected: codifies the non-repudiation hole. Test name `rollback_signed_session_verifies_from_audit_dir_bundle` would have to be renamed to assert wrong intent. |

The chosen Option A finishes the migration that Plan 22-05a Decision 5 started: audit-only sessions migrated in v2.2; `--rollback` sessions migrate in v2.3 Phase 27.2.

## Decision

The audit attestation bundle is written to `<audit_root>/<id>/audit-attestation.bundle` regardless of `--rollback` flag state. When `--rollback` is set, snapshot artifacts (`changes/`, `manifest.json`, etc.) continue to land under `<rollback_root>/<id>/` — only the bundle target moves.

`cmd_verify` resolves session lookup audit-first (`audit_session::load_session`) with a rollback-root fallback shim. On a fallback-root hit, the verifier emits exactly one `tracing::warn!` per process identifying both the legacy and canonical paths and the v2.5 removal milestone. The shim is removed in v2.5 (see `### Backward-compat shim contract` below; tracked as v2.5-FU-1 in `.planning/phases/27.1-nono-test-home-seam/deferred-items.md`).

## Consequences

### Positive

- **Non-repudiation closed.** `rm -rf ~/.nono/rollbacks/<id>/` no longer destroys the audit attestation bundle. Audit evidence outlives rollback cleanup.
- **Test 2 (`rollback_signed_session_verifies_from_audit_dir_bundle`) becomes correct by construction.** The test's `audit_dir.join("audit-attestation.bundle").exists()` assertion now passes for both `--rollback` and audit-only flows.
- **Plan 22-05a Decision 5's namespace separation is finished.** Audit-only sessions and rollback-active sessions both put their cryptographic record in the same canonical place. No more "depends on flags" routing.
- **Verifier's audit-first lookup is the natural shape post-migration.** The audit-aware loader at `audit_session.rs:160-198` was already designed for audit-first / rollback-fallback semantics; v2.3 Phase 27.2 simply wires it in.

### Negative

- **Verify path needs the rollback-root fallback shim until v2.5.** One extra `Path::exists` + `canonicalize` check on the cold path (when the audit-root lookup misses). Warm path (post-Phase-27.2 sessions) hits audit-root only — no perf regression.
- **Existing rollback-active bundles on disk fire the deprecation warning until re-signed.** Operators who upgrade from v2.2 to v2.3 see a one-shot `tracing::warn!` per process when verifying a pre-Phase-27.2 session. Documented in CHANGELOG (v2.3 entry) and via the warning text itself.
- **Two `create_dir_all` calls per `--rollback --audit-integrity --audit-sign-key` session** instead of one. One for `<audit_root>/<id>/` (bundle dir), one for `<rollback_root>/<id>/` (snapshot dir). Negligible perf cost (one extra `mkdir`-equivalent syscall).

### Backward-compat shim contract

- **Trigger:** `cmd_verify` finds the bundle at `<rollback_root>/<id>/audit-attestation.bundle` after missing it at `<audit_root>/<id>/audit-attestation.bundle`.
- **Action:** Verify proceeds normally (the bundle is still cryptographically valid; only its location is legacy). The verifier emits exactly one `tracing::warn!` per process identifying both the legacy and canonical paths and instructing the operator to re-sign affected sessions if they want to clear the warning.
- **Warning text** (locked in `audit_session::warn_once_legacy_bundle_path`): `"audit-attestation bundle found at legacy {legacy_path} path; canonical location is {canonical_path}. This compatibility shim is removed in v2.5 -- re-sign affected sessions if needed."`
- **Removal milestone:** v2.5-FU-1 (see `.planning/phases/27.1-nono-test-home-seam/deferred-items.md` § "Phase 27.2 v2.5 production follow-ups"). At v2.5 close, `cmd_verify` looks up bundles ONLY at `<audit_root>/<id>/audit-attestation.bundle`; the rollback-root fallback is deleted; the `warn_once_legacy_bundle_path` helper and its `OnceLock` guard are deleted.

## References

### Internal

- `.planning/REQUIREMENTS.md` § AAHX (REQ-AAHX-02 acceptance criteria)
- `.planning/phases/27.2-audit-attestation-test-re-enablement/27.2-CONTEXT.md` (decisions D-27.2-01, D-27.2-02, D-27.2-03, D-27.2-04, D-27.2-06)
- `.planning/phases/27.1-nono-test-home-seam/27.1-03-SUMMARY.md` (origin of FU-2; § "Gap 2 (Test 2 failure): Bundle target mismatch")
- `.planning/phases/27.1-nono-test-home-seam/deferred-items.md` § "Phase 27.2 v2.5 production follow-ups" (v2.5-FU-1 shim-removal milestone, v2.5-FU-2 JSON v2 schema)

### Source code

- `crates/nono-cli/src/audit_attestation.rs:155` (bundle write site: `bundle_path = session_dir.join(ATTESTATION_BUNDLE_FILENAME)`)
- `crates/nono-cli/src/rollback_runtime.rs::create_audit_state` (post-Phase-27.2: `session_dir` always derived from `audit_session::ensure_audit_session_dir`)
- `crates/nono-cli/src/audit_session.rs::load_session` (audit-first / rollback-fallback loader; live consumer added in Phase 27.2 Plan 01)
- `crates/nono-cli/src/audit_commands.rs::cmd_verify` (consumes the audit-aware loader post-Phase-27.2 Plan 01)

### Related ADRs

- `docs/architecture/aipc-unix-futures.md` (Phase 25-02 ADR convention reference; this ADR mirrors its structure)
```

2. Save the file. Verify the heading hierarchy with:
   ```bash
   grep -nE '^#{1,3} ' docs/architecture/audit-bundle-target.md
   ```
   Expected output (in order):
   - `# Audit Bundle Target`
   - `## Context`
   - `### Goals`
   - `### Non-goals`
   - `## Decision Table`
   - `## Decision`
   - `## Consequences`
   - `### Positive`
   - `### Negative`
   - `### Backward-compat shim contract`
   - `## References`
   - `### Internal`
   - `### Source code`
   - `### Related ADRs`

   If any heading is missing or out of order, FAIL the task and re-author from the scaffold.
  </action>
  <verify>
    <automated>test -f docs/architecture/audit-bundle-target.md &amp;&amp; echo FILE_EXISTS</automated>
    <automated>grep -c '^## Decision Table$' docs/architecture/audit-bundle-target.md</automated>
    <automated>grep -c 'Plan 22-05a Decision 5' docs/architecture/audit-bundle-target.md</automated>
    <automated>grep -c 'v2.5' docs/architecture/audit-bundle-target.md</automated>
    <automated>grep -c '### Backward-compat shim contract' docs/architecture/audit-bundle-target.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/architecture/audit-bundle-target.md` exits 0.
    - `wc -l docs/architecture/audit-bundle-target.md` returns at least 90 lines.
    - `grep -c '^## Decision Table$' docs/architecture/audit-bundle-target.md` returns exactly 1.
    - `grep -c 'Plan 22-05a Decision 5' docs/architecture/audit-bundle-target.md` returns >= 1 (supersession recorded).
    - `grep -c 'v2.5' docs/architecture/audit-bundle-target.md` returns >= 2 (Backward-compat shim contract section + Removal milestone reference).
    - `grep -c '### Backward-compat shim contract' docs/architecture/audit-bundle-target.md` returns exactly 1.
    - `grep -c '\*\*Status:\*\* Accepted' docs/architecture/audit-bundle-target.md` returns exactly 1.
    - `grep -c '\*\*Supersedes:\*\*' docs/architecture/audit-bundle-target.md` returns exactly 1.
    - `grep -c 'Option A' docs/architecture/audit-bundle-target.md` returns >= 1 (decision-table rows reference A).
    - `grep -c 'Rejected' docs/architecture/audit-bundle-target.md` returns >= 2 (Options B and C both rejected).
    - The markdown headings appear in the exact order specified in the action step.
  </acceptance_criteria>
  <done>
    `docs/architecture/audit-bundle-target.md` exists with the locked Status frontmatter (5 fields including `Supersedes`), the H2/H3 heading hierarchy in the order specified, the option matrix A/B/C with verdicts, the supersession note for Plan 22-05a Decision 5, the v2.5 milestone reference, and the explicit backward-compat shim contract subsection.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append v2.5-FU-1 + v2.5-FU-2 entries to deferred-items.md</name>
  <files>.planning/phases/27.1-nono-test-home-seam/deferred-items.md</files>
  <read_first>
    - .planning/phases/27.2-audit-attestation-test-re-enablement/27.2-PATTERNS.md (full read of section ".planning/phases/27.1-nono-test-home-seam/deferred-items.md — Append v2.5 entries", lines ~517-570 — the append scaffold)
    - .planning/phases/27.2-audit-attestation-test-re-enablement/27.2-CONTEXT.md § Deferred Ideas (v2.5-FU-1, v2.5-FU-2 spec)
    - .planning/phases/27.1-nono-test-home-seam/deferred-items.md (FULL FILE — confirms the existing § "Phase 27.1 Plan 03 v2.4 production follow-ups" section's heading shape so the new H2 mirrors it)
  </read_first>
  <action>
1. Open `.planning/phases/27.1-nono-test-home-seam/deferred-items.md`. Read the entire file. Locate the end of the existing `## Phase 27.1 Plan 03 v2.4 production follow-ups (Blocker 3 resurfaced)` section — confirm the file currently ends after the `### v2.4-FU-3` block (the `**Estimated effort:** 15 min including running the suite on a Windows host.` line at line 99).

2. Append (do NOT modify any existing content) a new section after the v2.4-FU-3 block. The append starts with a blank line, then this exact content:

```markdown

## Phase 27.2 v2.5 production follow-ups (audit-verify surface coordinated bump)

**Created during:** Phase 27.2 close (REQ-AAHX-01..03 closed via D-27.2-01..13)
**Status:** Surfaced; deferred to v2.5 milestone

### v2.5-FU-1: Remove `cmd_verify` back-compat shim

**Issue:** D-27.2-02 introduces a one-shot `tracing::warn!`-on-fallback shim in `audit_session::load_session` (consumed by `audit_commands::cmd_verify` per Phase 27.2 Plan 01) that lets verification succeed when bundles live at the legacy `<rollback>/<id>/audit-attestation.bundle` path (older `nono` versions, pre-Phase-27.2). The shim is intentionally narrow but adds a `Path::exists` + `canonicalize` check on the cold-path verify per session.

**Fix:** Hard cutover. `cmd_verify` (via `audit_session::load_session`) looks up bundles ONLY at `<audit_root>/<id>/audit-attestation.bundle`; the rollback-root fallback iteration is deleted. The `warn_once_legacy_bundle_path` helper and its `OnceLock<()>` guard are deleted. Pairs with v2.5-FU-2 below as a coordinated `cmd_verify` v2 schema bump (see `docs/architecture/audit-bundle-target.md` § "Backward-compat shim contract" for the locked removal milestone).

**Estimated effort:** 15-30 min (delete the rollback-root entry from the `roots` array in `audit_session::load_session`, delete the `warn_once_legacy_bundle_path` helper and its sole call site, delete any unit test that exercises the fallback path).

### v2.5-FU-2: `cmd_verify` v2 JSON schema (nested `attestation` object)

**Issue:** D-27.2-08 keeps `cmd_verify`'s flat JSON shape (`json["attestation_present"]`, `json["attestation_valid"]`) for Phase 27.2 to avoid coupling Test 2's re-enablement to a schema bump. The semantically richer nested shape (`json["attestation"]["present"]`, `json["attestation"]["signature_verified"]`, `json["attestation"]["merkle_root_matches"]`, `json["attestation"]["session_id_matches"]`, `json["attestation"]["verification_error"]`) was preserved in Test 2's original assertion intent (now adapted to the flat shape per Phase 27.2 Plan 04) and is the natural output once the audit-vs-rollback architectural split is fully landed.

**Fix:** Bump `cmd_verify` to a v2 schema with nested `attestation` object. Flat keys deprecated for one version with parallel emission, then removed in v2.6 (or per the deprecation policy decided at v2.5 scope-lock). Coordinate release with v2.5-FU-1 since both affect the audit-verify surface; recommend a single schema-version-bump commit landing both changes.

**Estimated effort:** 1-2 hours including schema-bump migration tests, `docs/cli/audit-verify.md` schema documentation update, and Test 2 re-asserting the nested shape.
```

3. Save the file. Verify the structural shape:
   - Two new H2 entries should NOT appear — only ONE new H2 (the v2.5 follow-ups section).
   - Two new H3 entries should appear (`### v2.5-FU-1`, `### v2.5-FU-2`).
   - The existing § "Phase 27.1 Plan 03 v2.4 production follow-ups" section MUST be untouched (no edits to its body or headings).

4. Confirm the file is well-formed Markdown (no orphan headers, no broken bullets) by reading the appended region back:
   ```bash
   tail -50 .planning/phases/27.1-nono-test-home-seam/deferred-items.md
   ```
   Should show the v2.5-FU-2 estimated effort line as the final line.
  </action>
  <verify>
    <automated>grep -c '^## Phase 27.2 v2.5 production follow-ups' .planning/phases/27.1-nono-test-home-seam/deferred-items.md</automated>
    <automated>grep -c '^### v2.5-FU-1:' .planning/phases/27.1-nono-test-home-seam/deferred-items.md</automated>
    <automated>grep -c '^### v2.5-FU-2:' .planning/phases/27.1-nono-test-home-seam/deferred-items.md</automated>
    <automated>grep -c 'audit-bundle-target.md' .planning/phases/27.1-nono-test-home-seam/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Phase 27.2 v2.5 production follow-ups' .planning/phases/27.1-nono-test-home-seam/deferred-items.md` returns exactly 1.
    - `grep -c '^### v2.5-FU-1:' .planning/phases/27.1-nono-test-home-seam/deferred-items.md` returns exactly 1.
    - `grep -c '^### v2.5-FU-2:' .planning/phases/27.1-nono-test-home-seam/deferred-items.md` returns exactly 1.
    - `grep -c 'audit-bundle-target.md' .planning/phases/27.1-nono-test-home-seam/deferred-items.md` returns >= 1 (cross-link from v2.5-FU-1 body to the ADR).
    - `grep -c '^## Phase 27.1 Plan 03 v2.4 production follow-ups' .planning/phases/27.1-nono-test-home-seam/deferred-items.md` STILL returns exactly 1 (existing section preserved).
    - `grep -c '^### v2.4-FU-3:' .planning/phases/27.1-nono-test-home-seam/deferred-items.md` STILL returns exactly 1 (existing v2.4 entries untouched).
    - The `### v2.5-FU-N:` entries each contain `**Issue:**`, `**Fix:**`, and `**Estimated effort:**` fields (style-invariant compliance with existing § "Phase 27.1 Plan 03 v2.4" entries).
  </acceptance_criteria>
  <done>
    `deferred-items.md` has a new H2 § "Phase 27.2 v2.5 production follow-ups" with two H3 entries (v2.5-FU-1 + v2.5-FU-2) following the existing style invariants. Existing v2.4 follow-up entries are untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify D-27.2-11 invariant + commit ADR + deferred-items append</name>
  <files>(verification + commit only — no further file changes)</files>
  <read_first>
    - .planning/phases/27.2-audit-attestation-test-re-enablement/27.2-CONTEXT.md (D-27.2-11 invariant)
    - CLAUDE.md § Commits (DCO sign-off mandatory)
  </read_first>
  <action>
1. Verify D-27.2-11: `git diff --stat -- crates/nono/` returns empty. The plan should have touched ONLY `docs/architecture/audit-bundle-target.md` (new file) and `.planning/phases/27.1-nono-test-home-seam/deferred-items.md` (modified). Confirm via:
   ```bash
   git status --short
   ```
   Expected: two lines, one for each file. No other files staged or unstaged.

2. Stage both files together (single commit since they're both part of the FU-2 documentation half):
   ```bash
   git add docs/architecture/audit-bundle-target.md .planning/phases/27.1-nono-test-home-seam/deferred-items.md
   ```

3. Commit with HEREDOC + DCO sign-off (per CLAUDE.md § Commits):
   ```
   docs(27.2-03): add audit-bundle-target ADR + v2.5 follow-up entries

   Author the architectural decision record at
   docs/architecture/audit-bundle-target.md recording the Option A bundle-target
   choice locked at /gsd-discuss-phase 27.2 (D-27.2-01..06; closes REQ-AAHX-02
   documentation half). The ADR mirrors the heading structure of the
   Phase 25-02 ADR (docs/architecture/aipc-unix-futures.md) and contains:

   - Status frontmatter with Supersedes: Plan 22-05a Decision 5
   - Option matrix A/B/C with verdicts and rationale
   - Backward-compat shim contract (warning text, removal milestone v2.5-FU-1)
   - Cross-references to source code touched by Phase 27.2 Plans 01 and 02

   Append v2.5-FU-1 (shim removal) and v2.5-FU-2 (cmd_verify v2 JSON schema)
   entries to .planning/phases/27.1-nono-test-home-seam/deferred-items.md
   per CONTEXT § Deferred Ideas. Both surface the v2.5 audit-verify surface
   coordinated bump.

   Refs: REQ-AAHX-02 (documentation half), D-27.2-01, D-27.2-03, D-27.2-11
   crates/nono/ byte-identical (D-27.2-11 invariant).
   ```

4. Run `git log -1 --format=%B | grep -c 'Signed-off-by'` to confirm DCO sign-off present. If absent, re-commit with `git commit -s --amend` per CLAUDE.md.

5. Final invariant check:
   ```bash
   git diff --stat HEAD~1 HEAD -- crates/nono/
   ```
   Must be empty.
  </action>
  <verify>
    <automated>git diff --stat HEAD~1 HEAD -- crates/nono/ | wc -l</automated>
    <automated>git log -1 --format=%B | grep -c 'Signed-off-by'</automated>
    <automated>git log -1 --name-only | grep -c 'audit-bundle-target.md'</automated>
    <automated>git log -1 --name-only | grep -c 'deferred-items.md'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat HEAD~1 HEAD -- crates/nono/ | wc -l` returns 0 (D-27.2-11 invariant intact).
    - `git log -1 --format=%B | grep -c 'Signed-off-by'` returns >= 1 (DCO sign-off present).
    - `git log -1 --name-only | grep -c 'audit-bundle-target.md'` returns 1 (ADR is in the commit).
    - `git log -1 --name-only | grep -c 'deferred-items.md'` returns 1 (deferred-items append is in the commit).
    - `git log -1 --format=%s` starts with `docs(27.2-03):` (conventional-commit type `docs:` because this plan changes documentation only).
    - `git log -1 --name-only | wc -l` shows exactly 2 file paths (no stray edits).
  </acceptance_criteria>
  <done>
    Both files committed in a single coherent docs commit with DCO sign-off; D-27.2-11 invariant intact; conventional-commit subject `docs(27.2-03):`. Plan 27.2-03 closes REQ-AAHX-02's documentation half; the implementation half is owned by Plan 27.2-02 (Wave 1, parallel).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → readers | The ADR is read by humans (project maintainers, security reviewers) and by future planning agents (next-milestone scope-lock). Risk class: misinformation. Mitigation: faithful rendering of CONTEXT.md decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.2-13 | Information Disclosure | ADR contents | accept | The ADR contains no secrets. All content is derived from already-public CONTEXT.md / PATTERNS.md / ROADMAP.md. Status field is `Accepted` (not draft) — the decision is locked, not under negotiation. |
| T-27.2-14 | Tampering (documentation drift) | ADR vs implementation divergence | mitigate | The ADR's "References → Source code" section names exact files (`audit_attestation.rs:155`, `rollback_runtime.rs::create_audit_state`, `audit_session.rs::load_session`, `audit_commands.rs::cmd_verify`). If a future commit refactors any of these, the ADR cross-reference can be updated as part of that refactor's commit. The shim-removal milestone (v2.5-FU-1) is itself a forcing function: when v2.5 lands, the ADR's Status field will flip to `Superseded` and a v2.5 ADR will record the cutover. |
| T-27.2-15 | Repudiation | Lossy decision capture | mitigate | The ADR records the option matrix A/B/C with explicit `Rejected` verdicts and rationale, so future readers see WHY this decision was made rather than only WHAT was decided. Mirrors the analog `aipc-unix-futures.md` shape. |

</threat_model>

<verification>
After all tasks land:
- `git log -1 --format=%s` starts with `docs(27.2-03):`.
- `docs/architecture/audit-bundle-target.md` is well-formed Markdown with all required sections.
- `.planning/phases/27.1-nono-test-home-seam/deferred-items.md` has the appended v2.5 follow-ups section.
- No source code touched; D-27.2-11 invariant trivially satisfied.
</verification>

<success_criteria>
1. ADR file exists at `docs/architecture/audit-bundle-target.md` with all required sections.
2. ADR records the option matrix A/B/C with explicit `Rejected` verdicts on B and C.
3. ADR explicitly supersedes Plan 22-05a Decision 5.
4. ADR documents the v2.5 shim-removal milestone (`v2.5-FU-1` reference + warning-text quote).
5. `deferred-items.md` has the new § "Phase 27.2 v2.5 production follow-ups" with v2.5-FU-1 + v2.5-FU-2 entries.
6. Single commit, conventional-commit subject `docs(27.2-03):`, DCO sign-off present.
7. `crates/nono/` byte-identical (trivially — no source touched).
</success_criteria>

<output>
After completion, create `.planning/phases/27.2-audit-attestation-test-re-enablement/27.2-03-SUMMARY.md` per `templates/summary.md`. Reference REQ-AAHX-02 (documentation half) in `requirements-completed`. Note that the implementation half lives in Plan 27.2-02's SUMMARY. Tag the commit hash from Task 3 in `task_commits`.
</output>
