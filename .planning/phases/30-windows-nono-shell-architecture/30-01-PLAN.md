---
phase: 30-windows-nono-shell-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/debug/nono-shell-status-dll-init-failed.md
autonomous: true
requirements:
  - D-10
  - D-08
  - D-09
tags:
  - windows
  - bookkeeping
  - debug-session-lifecycle

must_haves:
  truths:
    - "D-10: PROJECT.md SHELL-01 row no longer claims 'v2.0 Phase 08' as validated; reflects 'needs-rework' pending Phase 30 outcome"
    - "D-10: Bookkeeping correction is in scope regardless of Wave 1 technical outcome — even if cascade arm ships, the original v2.0 SHELL-01 'validated' claim was wrong (smoke gate did not include --profile claude-code end-to-end with WRITE_RESTRICTED + ConPTY)"
    - "D-08: Claude Code PreToolUse hook investigation explicitly remains OUT OF SCOPE — debug session 'claude-code-hook-not-firing' tracked separately"
    - "D-09: AppliedLabelsGuard leak (9 leaked Low-IL labels) explicitly remains OUT OF SCOPE — debug session 'nono-labels-guard-leak' tracked separately"
    - "Debug session 'nono-shell-status-dll-init-failed' frontmatter cites this CONTEXT.md as resolution_doc"
    - "STATE.md stopped_at updates to 'Phase 30 in flight: bookkeeping prelude complete, cascade edit pending'"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "SHELL-01 row flipped to needs-rework with debug-session reference"
      contains: "⚠ **SHELL-01**"
    - path: ".planning/STATE.md"
      provides: "stopped_at line updated to Phase 30; last_updated bumped"
      contains: "Phase 30"
    - path: ".planning/debug/nono-shell-status-dll-init-failed.md"
      provides: "frontmatter resolution_doc points at 30-CONTEXT.md (already partially set; this plan affirms the link and adds related_phase)"
      contains: "related_phase: 30-windows-nono-shell-architecture"
  key_links:
    - from: ".planning/PROJECT.md (SHELL-01 row)"
      to: ".planning/debug/nono-shell-status-dll-init-failed.md"
      via: "narrative reference in row description"
      pattern: "nono-shell-status-dll-init-failed"
    - from: ".planning/debug/nono-shell-status-dll-init-failed.md"
      to: ".planning/phases/30-windows-nono-shell-architecture/30-CONTEXT.md"
      via: "resolution_doc frontmatter field"
      pattern: "resolution_doc:.*30-CONTEXT.md"
---

<objective>
Bookkeeping prelude for Phase 30. Three independent file edits that can ship before any code change:

1. Flip `.planning/PROJECT.md`'s SHELL-01 row from `✔` validated to `⚠` needs-rework. The 2026-05-07 debug session invalidated the v2.0 Phase 08 "validated" claim because the smoke gate at the time did not exercise `--profile claude-code` end-to-end under the WRITE_RESTRICTED + ConPTY token shape that produces 0xC0000142.
2. Affirm the debug-session frontmatter wiring: `.planning/debug/nono-shell-status-dll-init-failed.md` already has `status: architecture-decided-pending-implementation` and `resolution_doc: .../30-CONTEXT.md`. This plan adds `related_phase: 30-windows-nono-shell-architecture` so the cross-link is symmetric. Annotate D-08 + D-09 follow-up debug sessions as sibling concerns NOT folded into Phase 30.
3. Update `.planning/STATE.md` to reflect that Phase 30 is in flight (Stopped At line + last_updated timestamp).

**This plan is outcome-independent of Wave 1 implementation** (D-10). Even if Plan 30-02's cascade arm fails on the test box and the phase ultimately ships as deferred-to-v3.0, the v2.0 SHELL-01 claim was wrong and gets corrected here. Plan 30-04 will flip the SHELL-01 row a second time (to validated OR deferred per outcome).

Purpose: Get the bookkeeping debt out of the way before any code change so the working tree state is honest about reality. Establishes the audit trail (debug-session ↔ phase ↔ PROJECT row) that downstream verification reads.
Output: Three modified planning files + a clean phase-start state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/phases/30-windows-nono-shell-architecture/30-CONTEXT.md
@.planning/phases/30-windows-nono-shell-architecture/30-PATTERNS.md
@.planning/debug/nono-shell-status-dll-init-failed.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip PROJECT.md SHELL-01 row to needs-rework</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (lines 60–80 — read the surrounding row context to mirror the bullet shape exactly)
    - .planning/phases/30-windows-nono-shell-architecture/30-CONTEXT.md (D-10 — the bookkeeping decision)
    - .planning/phases/30-windows-nono-shell-architecture/30-PATTERNS.md (lines 346–387 — pattern map for this exact row, including replacement text shape and surrounding-row examples)
    - .planning/debug/nono-shell-status-dll-init-failed.md (the source debug session — frontmatter only; do not need to read the body)
  </read_first>
  <action>
Replace the SHELL-01 row at PROJECT.md line 71 (currently `- ✔ **SHELL-01** — `nono shell` interactive ConPTY on Windows 10 17763+ — v2.0 Phase 08`) with the needs-rework form documented in 30-PATTERNS.md (Wave 1 first task subsection):

```
- ⚠ **SHELL-01** — `nono shell` interactive ConPTY on Windows 10 17763+ — v2.0 Phase 08 claim invalidated by 2026-05-07 debug session `nono-shell-status-dll-init-failed` (WRITE_RESTRICTED + ConPTY = 0xC0000142); needs-rework pending Phase 30 outcome
```

**Mechanical specifics:**
- Use Edit tool. Old string is the exact existing line including the leading `- ✔ ` and the trailing ` — v2.0 Phase 08`. New string is the line above (verbatim, including the `⚠` U+26A0 marker).
- Make NO other edits to PROJECT.md in this task. Plan 30-04 owns the second flip (outcome line: `✔ validated v2.X Phase 30` on success path OR `✘ deferred to v3.0` on failure path).
- The `⚠` marker is consistent with status markers used elsewhere in PROJECT.md (per 30-PATTERNS.md note about marker conventions). Do NOT introduce a new symbol.
- Do NOT modify rows 69, 70, 72, 73 (WRAP-01, SESS-01/02/03, PORT-01, PROXY-01) — they are intact and adjacent context.

**Why this row, this wording:**
- The old wording asserts validated-at-Phase-08; per D-10 that smoke gate did not include `--profile claude-code` under the production WRITE_RESTRICTED token shape. The new wording cites the specific 2026-05-07 debug session as the invalidation source and the trigger (`WRITE_RESTRICTED + ConPTY = 0xC0000142`). It does NOT prejudge the Phase 30 outcome.
- The `pending Phase 30 outcome` clause is what gets replaced in Plan 30-04. Keep the clause exactly so the second flip's grep pattern (`grep -n "pending Phase 30 outcome" .planning/PROJECT.md`) is precise.
  </action>
  <acceptance_criteria>
- `grep -E "⚠ \*\*SHELL-01\*\*.*nono-shell-status-dll-init-failed" .planning/PROJECT.md` returns exactly one match.
- `grep -E "✔ \*\*SHELL-01\*\*.*v2\.0 Phase 08" .planning/PROJECT.md` returns ZERO matches (old text fully removed).
- `grep -E "needs-rework pending Phase 30 outcome" .planning/PROJECT.md` returns exactly one match (anchor for Plan 30-04 second flip).
- `grep -nE "WRAP-01|SESS-01|PORT-01|PROXY-01" .planning/PROJECT.md | wc -l` returns the same count as before this edit (adjacent rows untouched).
- File is otherwise byte-identical: `git diff --stat .planning/PROJECT.md` shows ONE file changed, ONE insertion, ONE deletion (or two-and-two if line wraps differ, but the diff hunk should be one row).
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "if ((Select-String -Path .planning/PROJECT.md -Pattern '⚠ \*\*SHELL-01\*\*.*needs-rework pending Phase 30 outcome' -Encoding utf8).Count -eq 1 -and (Select-String -Path .planning/PROJECT.md -Pattern '✔ \*\*SHELL-01\*\*.*v2\.0 Phase 08' -Encoding utf8).Count -eq 0) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>SHELL-01 row reads `⚠` needs-rework with the exact 2026-05-07 debug-session reference. Old `✔ ... v2.0 Phase 08` line gone. Adjacent rows untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add related_phase to debug-session frontmatter</name>
  <files>.planning/debug/nono-shell-status-dll-init-failed.md</files>
  <read_first>
    - .planning/debug/nono-shell-status-dll-init-failed.md (lines 1–30 — frontmatter block; current shape is `status: architecture-decided-pending-implementation`, `resolution_doc: .../30-CONTEXT.md`, `related_phases: [14, 15, 17, 21, 27.1]`)
    - .planning/debug/resolved/windows-supervised-exec-cascade.md (lines 1–14 — Phase 15 reference frontmatter shape; shows `related_phase` singular convention used at resolution)
    - .planning/debug/resolved/supervisor-pipe-access-denied.md (lines 1–11 — second reference frontmatter shape)
    - .planning/phases/30-windows-nono-shell-architecture/30-PATTERNS.md (lines 476–540 — pattern map for debug-session lifecycle)
  </read_first>
  <action>
Add `related_phase: 30-windows-nono-shell-architecture` to the debug session's frontmatter. The file already has `related_phases: [14, 15, 17, 21, 27.1]` (plural list of historical context phases) — this plan adds the `related_phase` (singular) field naming the resolving phase, mirroring the convention in `windows-supervised-exec-cascade.md:11` and `supervisor-pipe-access-denied.md:9`.

**Where to insert:** Immediately after the existing `related_phases:` list line (line 22 in the current file). Keep `related_phases` plural intact (historical context); add `related_phase` singular alongside it.

**Mechanical specifics:**
- Use Edit tool with old string = `related_phases: [14, 15, 17, 21, 27.1]` and new string = the same line followed by a newline and `related_phase: 30-windows-nono-shell-architecture`.
- Do NOT change `status:` in this task. The status field at line 3 is already `architecture-decided-pending-implementation` — the correct intermediate state per CONTEXT.md (planner instruction: "flip it to architecture-decided-pending-implementation and reference this CONTEXT.md as the resolution"). Plan 30-04 owns the final flip to `status: resolved` plus the file move to `resolved/`.
- Do NOT touch `resolution_doc:` (already set), `checkpoint_outcome:`, `trigger:`, `created:`, etc.
- Do NOT add a `## Resolution` section yet — that comes at Wave close in Plan 30-04.

**Why this matters:** The cross-link makes the audit trail symmetric. PROJECT.md SHELL-01 row → debug session (via narrative reference). Debug session → Phase 30 CONTEXT.md (via `resolution_doc`). Debug session → Phase 30 itself (via new `related_phase`). Plan 30-04's grep gate `grep -E "related_phase: 30-windows-nono-shell-architecture" .planning/debug/resolved/nono-shell-status-dll-init-failed.md` reads this field after the move.
  </action>
  <acceptance_criteria>
- `grep -E "^related_phase: 30-windows-nono-shell-architecture" .planning/debug/nono-shell-status-dll-init-failed.md` returns exactly one match.
- `grep -E "^related_phases: \[14, 15, 17, 21, 27\.1\]" .planning/debug/nono-shell-status-dll-init-failed.md` returns exactly one match (historical-context plural list intact).
- `grep -E "^status: architecture-decided-pending-implementation" .planning/debug/nono-shell-status-dll-init-failed.md` returns exactly one match (status NOT yet flipped to resolved — that's Plan 30-04).
- `grep -E "^resolution_doc: \.planning/phases/30-windows-nono-shell-architecture/30-CONTEXT\.md" .planning/debug/nono-shell-status-dll-init-failed.md` returns exactly one match.
- File NOT moved (still at original path, not under `resolved/`). Plan 30-04 owns the move.
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "$f = '.planning/debug/nono-shell-status-dll-init-failed.md'; $a = (Select-String -Path $f -Pattern '^related_phase: 30-windows-nono-shell-architecture' -Encoding utf8).Count; $b = (Select-String -Path $f -Pattern '^status: architecture-decided-pending-implementation' -Encoding utf8).Count; $c = (Test-Path '.planning/debug/resolved/nono-shell-status-dll-init-failed.md'); if ($a -eq 1 -and $b -eq 1 -and -not $c) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>`related_phase: 30-windows-nono-shell-architecture` line present in frontmatter. `related_phases:` plural list and `status: architecture-decided-pending-implementation` unchanged. File still at non-resolved path.</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md stopped_at and last_updated for Phase 30 prelude</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (lines 1–15 — frontmatter block including `stopped_at`, `last_updated`, `last_activity`, `progress` fields; current `stopped_at: Phase 27.2 context gathered`, `last_updated: "2026-05-07T19:30:00.000Z"`)
    - .planning/STATE.md (lines 91–105 — Key Decisions blocks layout; this task does NOT add a Phase 30 entry yet — that's Plan 30-04 last task per 30-PATTERNS.md)
    - .planning/phases/30-windows-nono-shell-architecture/30-CONTEXT.md (D-10 — bookkeeping correction)
    - .planning/phases/30-windows-nono-shell-architecture/30-PATTERNS.md (lines 390–418 — STATE.md pattern map; this plan only updates the Stopped At + last_updated; the dense Key Decisions block belongs to Plan 30-04)
  </read_first>
  <action>
Two surgical edits to `.planning/STATE.md`:

**Edit 1 — `stopped_at` field (frontmatter line 6):**
Old: `stopped_at: Phase 27.2 context gathered`
New: `stopped_at: Phase 30 in flight — bookkeeping prelude complete, cascade edit pending (Plan 30-02)`

**Edit 2 — `last_updated` field (frontmatter line 7):**
Old: `last_updated: "2026-05-07T19:30:00.000Z"`
New: `last_updated: "2026-05-07T20:30:00.000Z"`

(Use a current-day ISO-8601 timestamp; the value above is illustrative. The point is to bump it past the existing 19:30 stamp by at least one minute. If the executor runs this hours later, use the actual current UTC timestamp — the gate just checks that it's lexicographically later than the prior value.)

**Mechanical specifics:**
- Use Edit tool. Two separate edits in the same file are fine in one Edit call only if both old strings appear exactly once (they do — they're frontmatter fields). Otherwise issue two Edit calls.
- Do NOT add a `### Key Decisions (v2.3)` Phase 30 entry in this task. Per 30-PATTERNS.md (lines 390–418), the dense narrative entry is owned by Plan 30-04's last task (it includes the OUTCOME, which is unknown until field smoke runs).
- Do NOT modify `progress:` block — `total_phases: 7`, `completed_phases: 4`, etc. stay the same. Phase 30 isn't complete; nothing to bump.
- Do NOT modify `last_activity:` (`2026-05-07`) — same calendar day; redundant to bump.
- Do NOT modify the `## Current Position` block (lines 21–25). Plan 30-04 owns the milestone-level narrative update.

**Why these two fields and not more:** STATE.md serves as session-continuity metadata. The frontmatter `stopped_at` is the field every GSD command reads when resuming. Updating it now means a fresh session correctly identifies Phase 30 as in flight. Anything heavier (Key Decisions narrative, progress bump) is premature — Wave 1 outcome decides what those entries say.
  </action>
  <acceptance_criteria>
- `grep -E "^stopped_at: Phase 30 in flight" .planning/STATE.md` returns exactly one match.
- `grep -E "^stopped_at: Phase 27\.2 context gathered" .planning/STATE.md` returns ZERO matches (old value fully removed).
- `grep -E "^last_updated: \"2026-05-07T(2[0-3]|19:[3-5][0-9]|19:5[1-9])" .planning/STATE.md` returns exactly one match (new timestamp lexicographically > old `19:30`); if executor uses a later wall-clock day, this regex tolerates any timestamp later in 2026-05-07T20+ or any subsequent day — adjust the regex to `^last_updated: "20[2-9][0-9]-` for full generality, but the principle is "newer than prior".
- `grep -cE "^### Key Decisions \(v2\.3\)" .planning/STATE.md` returns the same count as before this edit (Plan 30-04 owns the v2.3 Key Decisions block addition).
- `grep -nE "^stopped_at:" .planning/STATE.md` returns exactly one line (no duplicate frontmatter fields introduced).
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "$f = '.planning/STATE.md'; $a = (Select-String -Path $f -Pattern '^stopped_at: Phase 30 in flight' -Encoding utf8).Count; $b = (Select-String -Path $f -Pattern '^stopped_at: Phase 27\.2 context gathered' -Encoding utf8).Count; if ($a -eq 1 -and $b -eq 0) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>`stopped_at` reflects Phase 30 prelude state. `last_updated` is newer than prior. No premature Key Decisions narrative added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bookkeeping artifacts → human readers | Planning files are read by Claude (next session) and human auditors. Misleading bookkeeping creates false confidence. |
| Debug session metadata → GSD tooling | `.planning/debug/*.md` frontmatter is parsed by `gsd-sdk query` for status enumeration. Malformed YAML breaks the query layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information disclosure | PROJECT.md SHELL-01 row claiming validated when reality is broken | mitigate | Task 1 flips to ⚠ needs-rework with debug-session reference. The pre-Wave-1 state misled future planners; the corrected row tells the truth. |
| T-30-02 | Tampering (accidental) | YAML frontmatter syntax in `.planning/debug/nono-shell-status-dll-init-failed.md` | mitigate | Task 2 uses Edit tool with exact-string anchoring to prevent indentation drift. Acceptance gate verifies the resulting key parses (regex `^related_phase:` requires correct indentation). |
| T-30-03 | Repudiation | Loss of audit trail between PROJECT.md row, debug session, and Phase 30 CONTEXT.md | mitigate | Task 1 + Task 2 establish symmetric cross-links. Task 2's `related_phase` field + the existing `resolution_doc` field make the trail bidirectional. |
| T-30-04 | Tampering | Premature Phase 30 narrative in STATE.md before outcome is known | mitigate | Task 3 explicitly excludes the v2.3 Key Decisions block. Plan 30-04 (after field smoke) writes the dense narrative with the actual outcome. Acceptance gate verifies the block is NOT yet added. |
</threat_model>

<verification>
- All three files modified per task acceptance criteria.
- Grep patterns above all green.
- `git status` shows three modified files (no other changes).
- `git diff` shows: PROJECT.md ±1 line in the SHELL-01 row; debug session +1 line in frontmatter; STATE.md ±2 lines in frontmatter.
</verification>

<success_criteria>
- D-10 partially closed (first half: SHELL-01 → needs-rework). Plan 30-04 closes the second half (→ validated OR deferred).
- D-08 + D-09 explicitly recorded as out-of-scope sibling concerns (encoded in must_haves.truths and in PROJECT.md row narrative).
- Audit trail symmetric: PROJECT row ↔ debug session ↔ Phase 30 CONTEXT.
- STATE.md fresh-resume points at Phase 30 (not Phase 27.2).
</success_criteria>

<output>
After completion, create `.planning/phases/30-windows-nono-shell-architecture/30-01-SUMMARY.md` per the template at @$HOME/.claude/get-shit-done/templates/summary.md. Note explicitly:
- This plan ships the bookkeeping prelude. The cascade edit (Plan 30-02), field-smoke harness (Plan 30-03), and outcome-flip (Plan 30-04) follow in subsequent waves.
- Wave 2 (Plan 30-05) is conditional on Plan 30-04 outcome.
- D-10 second-half close is owned by Plan 30-04.
</output>
